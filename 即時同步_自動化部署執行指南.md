# 即時同步 - 自動化部署執行指南

> **執行日期**: 2025-10-16  
> **Supabase 專案**: https://vlyhwegpvpnjyocqmfqc.supabase.co  
> **工作目錄**: d:\repo

---

## 🎯 執行概覽

本指南將引導您完成以下步驟：

1. ✅ **部署到測試環境** - 執行 Migration 和前置檢查
2. ✅ **執行測試** - 驗證功能正常運作
3. ✅ **啟用即時同步** - 開啟即時通知功能
4. ✅ **監控運行狀態** - 檢查性能指標

**預計執行時間**: 15-20 分鐘

---

## 📍 步驟 1: 部署到測試環境

### 1.1 打開 Supabase SQL Editor

1. 訪問 Supabase Dashboard:
   ```
   https://app.supabase.com/project/vlyhwegpvpnjyocqmfqc/sql
   ```

2. 登入您的 Supabase 帳號

3. 確認您在正確的專案中（vlyhwegpvpnjyocqmfqc）

### 1.2 執行 Migration 腳本

**腳本位置**: `d:\repo\supabase\migrations\20251016_create_realtime_sync_trigger.sql`

**執行步驟**:

1. 在 SQL Editor 中點擊 **"New query"**

2. 複製以下腳本內容（或直接打開文件複製）:

   **方式 1**: 在本地打開文件
   ```
   文件路徑: d:\repo\supabase\migrations\20251016_create_realtime_sync_trigger.sql
   ```

   **方式 2**: 使用以下命令查看內容
   ```bash
   # 在 d:\repo 目錄下執行
   cat supabase/migrations/20251016_create_realtime_sync_trigger.sql
   ```

3. 將腳本內容貼到 SQL Editor

4. 點擊 **"Run"** 按鈕執行

**預期結果**:
```
✅ CREATE EXTENSION
✅ CREATE FUNCTION notify_edge_function_realtime
✅ CREATE TABLE realtime_sync_stats
✅ INSERT INTO system_settings (realtime_sync_config)
✅ CREATE FUNCTION get_realtime_sync_status
```

**如果出現錯誤**:
- 檢查是否有權限執行 DDL 語句
- 確認 system_settings 表存在
- 查看錯誤訊息並記錄

### 1.3 驗證 Migration 執行結果

在 SQL Editor 中執行以下驗證查詢:

```sql
-- 驗證 1: 檢查 Trigger Function 是否創建
SELECT 
  proname AS "函數名稱",
  prosrc AS "函數內容（前100字）"
FROM pg_proc 
WHERE proname = 'notify_edge_function_realtime';

-- 預期結果: 應該返回 1 行，顯示函數名稱

-- 驗證 2: 檢查配置是否創建
SELECT * FROM system_settings 
WHERE key = 'realtime_sync_config';

-- 預期結果: 應該返回 1 行，value 包含 {"enabled": false}

-- 驗證 3: 檢查狀態函數
SELECT * FROM get_realtime_sync_status();

-- 預期結果: 返回系統狀態，trigger_exists 應為 false
```

**✅ 檢查點**: 
- [ ] Trigger Function 已創建
- [ ] 配置已插入 system_settings
- [ ] 狀態函數可正常執行

### 1.4 檢查 pg_net 擴展

執行以下查詢:

```sql
-- 檢查 pg_net 擴展
SELECT * FROM pg_extension WHERE extname = 'pg_net';
```

**如果返回空結果（pg_net 未啟用）**:

1. 前往 Supabase Dashboard:
   ```
   https://app.supabase.com/project/vlyhwegpvpnjyocqmfqc/database/extensions
   ```

2. 在搜尋框中輸入 "pg_net"

3. 找到 pg_net 擴展，點擊 **"Enable"** 按鈕

4. 等待幾秒鐘，直到狀態變為 "Enabled"

5. 返回 SQL Editor，重新執行檢查查詢

**✅ 檢查點**: 
- [ ] pg_net 擴展已啟用

### 1.5 檢查 Edge Function 狀態

1. 前往 Edge Functions 頁面:
   ```
   https://app.supabase.com/project/vlyhwegpvpnjyocqmfqc/functions
   ```

2. 找到 `sync-to-firestore` 函數

3. 檢查狀態:
   - ✅ 狀態應為 "Active" 或 "Deployed"
   - ✅ 最近有執行記錄
   - ✅ 沒有錯誤訊息

**✅ 檢查點**: 
- [ ] Edge Function 正常運行

### 1.6 檢查 Cron Job 狀態

在 SQL Editor 中執行:

```sql
-- 檢查 Cron Job
SELECT 
  jobname AS "任務名稱",
  schedule AS "執行頻率",
  active AS "是否啟用",
  CASE 
    WHEN active THEN '✅ 正常運行'
    ELSE '❌ 未啟用'
  END AS "狀態"
FROM cron.job
WHERE jobname = 'sync-orders-to-firestore';

-- 檢查最近執行記錄
SELECT 
  status AS "狀態",
  start_time AS "開始時間",
  end_time AS "結束時間"
FROM cron.job_run_details
WHERE jobid = (
  SELECT jobid FROM cron.job WHERE jobname = 'sync-orders-to-firestore'
)
ORDER BY start_time DESC
LIMIT 5;
```

**預期結果**:
- Cron Job 存在且 active = true
- 最近有執行記錄（status = 'succeeded'）

**✅ 檢查點**: 
- [ ] Cron Job 正常運行

---

## 📍 步驟 2: 執行測試

### 2.1 執行測試腳本

**腳本位置**: `d:\repo\supabase\test_realtime_sync.sql`

**執行步驟**:

1. 在 SQL Editor 中新建查詢

2. 打開測試腳本文件:
   ```
   文件路徑: d:\repo\supabase\test_realtime_sync.sql
   ```

3. 複製完整內容到 SQL Editor

4. 點擊 **"Run"** 執行

**測試內容**:
- ✅ 前置條件檢查（pg_net、Trigger Function、配置）
- ✅ 創建測試訂單
- ✅ 驗證 Outbox 事件創建
- ✅ 檢查 HTTP 請求（如果即時同步已啟用）
- ✅ 更新測試訂單
- ✅ 檢查 Cron Job 狀態
- ✅ 性能統計

### 2.2 查看測試結果

測試腳本會輸出多個結果集，請檢查:

**測試 1: 前置條件檢查**
```
✅ pg_net 擴展已啟用
✅ Trigger Function 已創建
✅ 配置已創建
```

**測試 2: 創建測試訂單**
```
✅ 測試訂單已創建: [UUID]
⏱️  創建時間: [時間戳]
✅ Outbox 事件已創建
```

**測試 3: 更新測試訂單**
```
✅ 測試訂單已更新: [UUID]
```

**測試 4: Cron Job 檢查**
```
任務名稱: sync-orders-to-firestore
執行頻率: */5 * * * * (或 */30 * * * *)
是否啟用: true
狀態: ✅ 正常運行
```

**測試 5: 性能統計**
```
總事件數: [數字]
已處理: [數字]
待處理: [數字]
錯誤: [數字]
平均延遲（秒）: [數字]
```

**✅ 檢查點**: 
- [ ] 所有前置條件檢查通過
- [ ] 測試訂單創建成功
- [ ] Outbox 事件正常創建
- [ ] Cron Job 正常運行
- [ ] 性能指標正常

### 2.3 清理測試數據（可選）

如果需要清理測試數據，執行:

```sql
-- 刪除測試訂單
DELETE FROM bookings 
WHERE pickup_location LIKE '%即時同步測試%';

-- 刪除對應的 outbox 事件
DELETE FROM outbox 
WHERE aggregate_id IN (
  SELECT id::TEXT FROM bookings 
  WHERE pickup_location LIKE '%即時同步測試%'
);
```

---

## 📍 步驟 3: 啟用即時同步

### 3.1 執行啟用腳本

**腳本位置**: `d:\repo\supabase\enable_realtime_sync.sql`

**執行步驟**:

1. 在 SQL Editor 中新建查詢

2. 打開啟用腳本:
   ```
   文件路徑: d:\repo\supabase\enable_realtime_sync.sql
   ```

3. 複製完整內容到 SQL Editor

4. 點擊 **"Run"** 執行

**預期輸出**:
```
✅ Trigger Function 存在
✅ pg_net 擴展已啟用
✅ 舊 Trigger 已刪除（如果存在）
✅ 新 Trigger 已創建
✅ 配置已更新為 enabled=true
```

### 3.2 驗證 Trigger 已創建

執行驗證查詢:

```sql
-- 驗證 1: 檢查 Trigger 是否存在
SELECT 
  tgname AS "Trigger 名稱",
  tgenabled AS "是否啟用",
  CASE 
    WHEN tgenabled = 'O' THEN '✅ 已啟用'
    ELSE '❌ 未啟用'
  END AS "狀態"
FROM pg_trigger 
WHERE tgname = 'bookings_realtime_notify_trigger';

-- 預期結果: 返回 1 行，狀態為 "✅ 已啟用"

-- 驗證 2: 檢查配置
SELECT 
  key,
  value->>'enabled' AS "是否啟用",
  value->>'enabled_at' AS "啟用時間"
FROM system_settings 
WHERE key = 'realtime_sync_config';

-- 預期結果: enabled = "true"

-- 驗證 3: 使用狀態函數
SELECT * FROM get_realtime_sync_status();

-- 預期結果: 
-- trigger_exists = true
-- config_enabled = true
```

**✅ 檢查點**: 
- [ ] Trigger 已創建並啟用
- [ ] 配置已更新為 enabled=true
- [ ] 狀態函數返回正確狀態

### 3.3 測試即時通知

創建一個新的測試訂單，驗證即時通知:

```sql
-- 記錄開始時間
SELECT NOW() AS test_start_time;

-- 創建測試訂單
INSERT INTO bookings (
  customer_id,
  status,
  pickup_location,
  destination,
  start_date,
  start_time,
  duration_hours,
  vehicle_type,
  total_amount,
  deposit_amount
) VALUES (
  (SELECT id FROM users WHERE role = 'customer' LIMIT 1),
  'pending',
  '即時通知測試 - ' || NOW()::TEXT,
  '目的地測試',
  CURRENT_DATE + INTERVAL '1 day',
  '14:00:00',
  8,
  'A',
  1000.00,
  300.00
) RETURNING id, created_at;

-- 等待 3 秒後檢查 HTTP 請求
-- (需要手動等待 3 秒，然後執行下面的查詢)

-- 檢查最近的 HTTP 請求
SELECT 
  id AS "請求 ID",
  status_code AS "狀態碼",
  created AS "請求時間",
  EXTRACT(EPOCH FROM (created - (SELECT test_start_time FROM (SELECT NOW() - INTERVAL '10 seconds' AS test_start_time) t))) AS "延遲（秒）"
FROM net._http_response
WHERE created >= NOW() - INTERVAL '10 seconds'
  AND url LIKE '%sync-to-firestore%'
ORDER BY created DESC
LIMIT 1;
```

**預期結果**:
- 應該看到新的 HTTP 請求記錄
- status_code = 200
- 延遲時間 < 3 秒

**✅ 檢查點**: 
- [ ] HTTP 請求已發送
- [ ] 狀態碼為 200
- [ ] 延遲時間 < 3 秒

---

## 📍 步驟 4: 監控運行狀態

### 4.1 執行狀態檢查腳本

**腳本位置**: `d:\repo\supabase\check_realtime_sync_status.sql`

**執行步驟**:

1. 在 SQL Editor 中新建查詢

2. 打開狀態檢查腳本:
   ```
   文件路徑: d:\repo\supabase\check_realtime_sync_status.sql
   ```

3. 複製完整內容到 SQL Editor

4. 點擊 **"Run"** 執行

### 4.2 查看關鍵指標

狀態檢查腳本會輸出 11 個檢查項目:

**檢查 1: Trigger 狀態**
```
Trigger 名稱: bookings_realtime_notify_trigger
是否啟用: ✅ 已啟用
```

**檢查 2: Trigger Function 狀態**
```
函數名稱: notify_edge_function_realtime
狀態: ✅ 存在
```

**檢查 3: pg_net 擴展**
```
擴展名稱: pg_net
狀態: ✅ 已啟用
```

**檢查 4: 配置狀態**
```
是否啟用: true
啟用時間: [時間戳]
```

**檢查 5: Cron Job 狀態**
```
任務名稱: sync-orders-to-firestore
是否啟用: ✅ 正常運行
```

**檢查 6-11: 性能指標**
```
今日總事件數: [數字]
今日已處理: [數字]
今日待處理: [數字]
今日錯誤數: [數字]
平均延遲（秒）: [數字]
HTTP 成功率: [百分比]
```

### 4.3 性能評估

根據指標評估系統性能:

**優秀** (Excellent):
- 平均延遲 < 3 秒
- HTTP 成功率 > 95%
- 待處理事件 < 5 個
- 錯誤率 < 1%

**良好** (Good):
- 平均延遲 < 10 秒
- HTTP 成功率 > 90%
- 待處理事件 < 10 個
- 錯誤率 < 5%

**需優化** (Needs Improvement):
- 平均延遲 > 10 秒
- HTTP 成功率 < 90%
- 待處理事件 > 10 個
- 錯誤率 > 5%

**✅ 檢查點**: 
- [ ] 平均延遲符合預期
- [ ] HTTP 成功率 > 95%
- [ ] 待處理事件數正常
- [ ] 錯誤率低

---

## 🎉 部署完成

恭喜！如果所有檢查點都通過，即時同步功能已成功部署並運行。

### 後續步驟

1. **持續監控**
   - 每天執行一次狀態檢查腳本
   - 關注性能指標變化
   - 及時處理錯誤事件

2. **訪問管理界面**
   ```
   http://localhost:3001/settings/dispatch
   ```
   - 查看實時狀態
   - 查看統計圖表
   - 使用開關控制即時同步

3. **如需停用**
   - 執行 `disable_realtime_sync.sql`
   - 或通過管理界面關閉開關

---

## 📞 故障排除

如遇到問題，請參考:

1. **開發文檔**: `docs/20251016_1430_04_即時補償雙保險通知架構.md`
2. **README**: `即時補償雙保險通知架構_README.md`
3. **部署檢查清單**: `即時同步部署檢查清單.md`

---

**執行指南版本**: v1.0.0  
**創建日期**: 2025-10-16  
**維護者**: 開發團隊

