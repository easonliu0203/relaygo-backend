# 取消訂單功能 - 完整測試指南

**日期**：2025-10-08  
**修復**：取消訂單 API 模組導入錯誤 + Flutter 客戶端錯誤  
**狀態**：✅ 已修復，待測試

---

## 📋 修復摘要

### 問題 1：管理後台 API 錯誤（已修復）⭐

**錯誤**：
```
Module not found: Can't resolve '@/lib/database'
```

**修復**：
- 將 `@/lib/database` 改為 `@/lib/supabase`
- 文件：`web-admin/src/app/api/bookings/[id]/cancel/route.ts`

---

### 問題 2：Flutter 客戶端錯誤（已修復）

**錯誤**：
```
'_dependents.isEmpty': is not true
```

**修復**：
- 使用 `.then()` 回調在對話框關閉後 dispose controller
- 文件：`mobile/lib/apps/customer/presentation/pages/order_detail_page.dart`

---

## 🚀 測試前準備

### 1. 重啟管理後台（重要）⭐

**為什麼需要重啟**：
- 修改了 API 路由文件
- Next.js 需要重新編譯

**步驟**：
```bash
# 停止管理後台（在管理後台終端按 Ctrl+C）

# 重新啟動
cd web-admin
npm run dev
```

**確認成功**：
```
✓ Ready in 2.5s
○ Local:        http://localhost:3001
```

**如果看到錯誤**：
```
Error: Module not found: Can't resolve '@/lib/database'
```
→ 代碼沒有正確更新，請檢查文件

---

### 2. 重新建置 Flutter App

**為什麼需要重新建置**：
- 修改了 Flutter 代碼
- 需要重新編譯

**步驟**：
```bash
cd mobile
flutter clean
flutter pub get
flutter run --flavor customer --target lib/apps/customer/main_customer.dart
```

---

### 3. 確認 Supabase 和 Firestore 正常運行

**Supabase**：
- ✅ 專案：vlyhwegpvpnjyocqmfqc
- ✅ Edge Function：sync-to-firestore
- ✅ Cron Job：每 30 秒執行

**Firestore**：
- ✅ 規則已部署
- ✅ 允許讀取不存在的文檔

---

## ✅ 測試步驟

### 測試 1：管理後台 API 端點測試（最重要）⭐

#### 目的
確認 API 可以正常編譯並返回 JSON。

#### 步驟

**測試 1.1：測試不存在的訂單**
```bash
curl -X POST http://localhost:3001/api/bookings/test-id/cancel \
  -H "Content-Type: application/json" \
  -d '{
    "customerUid": "test-uid",
    "reason": "測試取消功能"
  }'
```

**預期結果**：
```json
{
  "success": false,
  "error": "訂單不存在"
}
```

**關鍵驗證**：
- ✅ 返回 JSON（不是 HTML）
- ✅ 狀態碼 404
- ✅ 錯誤訊息清晰

---

**測試 1.2：測試缺少必要欄位**
```bash
curl -X POST http://localhost:3001/api/bookings/test-id/cancel \
  -H "Content-Type: application/json" \
  -d '{
    "customerUid": "test-uid"
  }'
```

**預期結果**：
```json
{
  "success": false,
  "error": "缺少必要欄位: customerUid 或 reason"
}
```

---

**測試 1.3：測試取消原因太短**
```bash
curl -X POST http://localhost:3001/api/bookings/test-id/cancel \
  -H "Content-Type: application/json" \
  -d '{
    "customerUid": "test-uid",
    "reason": "測試"
  }'
```

**預期結果**：
```json
{
  "success": false,
  "error": "取消原因至少需要 5 個字元"
}
```

---

#### 如果測試失敗

**錯誤 1：仍然返回 HTML**
```html
<!DOCTYPE html>
<html>
  <head>
    <title>Error</title>
  </head>
  ...
</html>
```

**原因**：
- 代碼沒有正確更新
- 管理後台沒有重新編譯

**解決方案**：
1. 確認文件已修改：
   ```bash
   cat web-admin/src/app/api/bookings/[id]/cancel/route.ts | grep "lib/supabase"
   ```
2. 重啟管理後台
3. 重新測試

---

**錯誤 2：連接失敗**
```
curl: (7) Failed to connect to localhost port 3001
```

**原因**：
- 管理後台未運行

**解決方案**：
```bash
cd web-admin
npm run dev
```

---

### 測試 2：完整取消訂單流程

#### 步驟

**2.1 創建測試訂單**
1. 啟動 Flutter App
2. 登入測試帳號
3. 創建新訂單：
   - 上車地點：台北車站
   - 下車地點：台北 101
   - 預約時間：明天 10:00
   - 乘客人數：2 人
4. 點擊「確認預約」

---

**2.2 支付訂金**
1. 選擇支付方式
2. 點擊「確認支付」
3. 等待跳轉到「預約成功」頁面

---

**2.3 進入訂單詳情**
1. 點擊「查看訂單詳情」
2. 確認訂單狀態為「待配對」或「已配對」

---

**2.4 取消訂單**
1. 點擊「取消訂單」按鈕
2. 應該顯示取消訂單對話框
3. 輸入取消原因：「測試取消功能，驗證修復效果」
4. 點擊「確認取消」

---

#### 預期結果

**對話框行為**：
- ✅ 對話框正常關閉（有關閉動畫）
- ✅ **不出現 `_dependents.isEmpty` 錯誤** ⭐
- ✅ **不出現任何 Flutter 框架錯誤** ⭐

**API 調用**（查看控制台日誌）：
```
[BookingService] 開始取消訂單: xxx
[BookingService] 請求 URL: http://10.0.2.2:3001/api/bookings/xxx/cancel
[BookingService] 響應狀態碼: 200
[BookingService] 響應 Content-Type: application/json
[BookingService] 響應內容: {"success":true,"message":"訂單已成功取消",...}
[BookingService] 取消成功
```

**UI 更新**：
- ✅ 顯示「訂單已取消」SnackBar
- ✅ 訂單狀態更新為「已取消」
- ✅ 「取消訂單」按鈕消失或禁用

**資料同步**：
- ✅ Supabase `bookings` 表狀態更新為 `cancelled`
- ✅ Supabase `outbox` 表有新事件
- ✅ 等待 30 秒後，Firestore 同步完成

---

#### 如果測試失敗

**錯誤 1：仍然出現 `_dependents.isEmpty` 錯誤**

**原因**：
- Flutter 代碼沒有正確更新
- App 沒有重新建置

**解決方案**：
1. 確認代碼已修改：
   ```bash
   cat mobile/lib/apps/customer/presentation/pages/order_detail_page.dart | grep -A 5 ".then((reason)"
   ```
2. 執行 `flutter clean`
3. 重新運行 App
4. 重新測試

---

**錯誤 2：顯示「API 返回非 JSON 響應」**

**原因**：
- 管理後台 API 仍然有問題
- 管理後台沒有重新編譯

**解決方案**：
1. 查看管理後台終端
2. 確認沒有編譯錯誤
3. 重啟管理後台
4. 重新測試

---

**錯誤 3：顯示「取消失敗：...」**

**查看日誌**：
```
[BookingService] 響應狀態碼: 400/403/404/500
[BookingService] 響應內容: {"error":"..."}
```

**根據狀態碼診斷**：
- 400 → 驗證錯誤（檢查取消原因長度）
- 403 → 權限錯誤（檢查用戶 UID）
- 404 → 訂單不存在（檢查訂單 ID）
- 500 → API 內部錯誤（檢查管理後台日誌）

---

### 測試 3：驗證資料同步

#### 步驟

**3.1 檢查 Supabase `bookings` 表**
1. 打開 Supabase Dashboard
2. 前往 Table Editor
3. 選擇 `bookings` 表
4. 找到測試訂單

**預期資料**：
```
status: cancelled
cancellation_reason: 測試取消功能，驗證修復效果
cancelled_at: 2025-10-08 20:47:00+00
```

---

**3.2 檢查 Supabase `outbox` 表**
```sql
SELECT 
  id,
  aggregate_type,
  aggregate_id,
  event_type,
  payload->>'status' as status,
  created_at,
  processed_at
FROM outbox
WHERE aggregate_id = '{訂單ID}'
ORDER BY created_at DESC
LIMIT 5;
```

**預期資料**：
```
aggregate_type: booking
event_type: updated
status: cancelled
processed_at: NULL (待處理) 或 有時間戳（已處理）
```

---

**3.3 檢查 Firestore**
1. 打開 Firebase Console
2. 前往 Firestore Database
3. 查看 `orders_rt/{訂單ID}`
4. 查看 `bookings/{訂單ID}`

**預期資料**：
```
status: cancelled
cancellationReason: 測試取消功能，驗證修復效果
cancelledAt: Timestamp
```

---

### 測試 4：多次取消測試

#### 目的
確認 TextEditingController 正確釋放，沒有記憶體洩漏。

#### 步驟
1. 進入訂單詳情頁面
2. 點擊「取消訂單」按鈕
3. 點擊「不取消」
4. 再次點擊「取消訂單」按鈕
5. 點擊「不取消」
6. 重複 3-5 次

#### 預期結果
- ✅ 每次都能正常打開對話框
- ✅ 每次都能正常關閉對話框
- ✅ **不出現任何錯誤** ⭐
- ✅ 不出現記憶體洩漏

---

## 🔍 故障排除

### 問題 1：管理後台編譯錯誤

**症狀**：
```
Error: Module not found: Can't resolve '@/lib/database'
```

**解決方案**：
1. 確認文件已修改：
   ```bash
   grep "lib/supabase" web-admin/src/app/api/bookings/[id]/cancel/route.ts
   ```
2. 如果沒有，手動修改第 2 行
3. 重啟管理後台

---

### 問題 2：API 返回 HTML

**症狀**：
```
[BookingService] 響應 Content-Type: text/html
[BookingService] 響應內容: <!DOCTYPE html>...
```

**解決方案**：
1. 檢查管理後台終端
2. 查看是否有編譯錯誤
3. 重啟管理後台
4. 重新測試

---

### 問題 3：Flutter 客戶端錯誤

**症狀**：
```
'_dependents.isEmpty': is not true
```

**解決方案**：
1. 確認代碼已修改
2. 執行 `flutter clean`
3. 重新運行 App
4. 重新測試

---

### 問題 4：訂單狀態不允許取消

**症狀**：
```
{
  "error": "訂單狀態為 completed，無法取消"
}
```

**原因**：
- 只能取消 `pending` 或 `matched` 狀態的訂單

**解決方案**：
- 創建新的測試訂單
- 確保訂單狀態為 `pending` 或 `matched`

---

## 📊 測試檢查清單

### 管理後台 API
- [ ] 管理後台正常啟動（沒有編譯錯誤）
- [ ] API 端點返回 JSON（不是 HTML）
- [ ] 測試不存在的訂單（返回 404）
- [ ] 測試缺少必要欄位（返回 400）
- [ ] 測試取消原因太短（返回 400）

### Flutter 客戶端
- [ ] App 重新建置
- [ ] 創建測試訂單
- [ ] 支付訂金
- [ ] 進入訂單詳情
- [ ] 點擊「取消訂單」
- [ ] 輸入取消原因
- [ ] 點擊「確認取消」

### 錯誤檢查
- [ ] 不出現 `_dependents.isEmpty` 錯誤 ⭐
- [ ] 不出現 `FormatException` 錯誤
- [ ] 不出現「API 返回非 JSON 響應」錯誤
- [ ] 對話框正常關閉

### 功能驗證
- [ ] 顯示「訂單已取消」訊息
- [ ] 訂單狀態更新為「已取消」
- [ ] Supabase 資料已更新
- [ ] Firestore 資料已同步

### 日誌驗證
- [ ] 看到「請求 URL」日誌
- [ ] 看到「響應狀態碼: 200」日誌
- [ ] 看到「響應 Content-Type: application/json」日誌
- [ ] 看到「取消成功」日誌

---

## 💡 測試技巧

### 1. 使用 Flutter DevTools

```bash
flutter run --flavor customer --target lib/apps/customer/main_customer.dart --devtools
```

**查看**：
- Logging（查看所有日誌）
- Memory（檢查記憶體洩漏）
- Widget Inspector（檢查 Widget 樹）

---

### 2. 使用 curl 測試 API

**優點**：
- 快速測試 API 端點
- 不需要啟動 Flutter App
- 可以測試各種錯誤情況

**範例**：
```bash
# 測試成功情況（需要真實的訂單 ID）
curl -X POST http://localhost:3001/api/bookings/{真實訂單ID}/cancel \
  -H "Content-Type: application/json" \
  -d '{
    "customerUid": "{真實用戶UID}",
    "reason": "測試取消功能"
  }'

# 測試錯誤情況
curl -X POST http://localhost:3001/api/bookings/test-id/cancel \
  -H "Content-Type: application/json" \
  -d '{
    "customerUid": "test-uid",
    "reason": "測試"
  }'
```

---

### 3. 查看管理後台日誌

**Next.js 開發伺服器日誌**：
- 查看管理後台終端
- 尋找 API 請求日誌
- 檢查錯誤堆棧追蹤

**範例日誌**：
```
🚫 收到取消訂單請求: { bookingId: 'xxx', customerUid: 'xxx', reason: '...' }
📋 找到訂單: { id: 'xxx', status: 'matched', customerUid: 'xxx' }
✅ 訂單已取消: { id: 'xxx', status: 'cancelled', cancelledAt: '...' }
```

---

### 4. 使用 Postman 或 Insomnia

**優點**：
- 圖形化界面
- 可以保存請求
- 可以查看詳細的響應

---

## 📈 預期時間線

### 正常流程時間線

```
T+0s:  用戶點擊「取消訂單」按鈕
T+0s:  顯示取消訂單對話框
T+5s:  用戶輸入取消原因
T+10s: 用戶點擊「確認取消」
T+10s: 驗證取消原因（至少 5 個字元）✅
T+10s: Navigator.pop(reason) - 開始關閉對話框
T+10.3s: 對話框關閉動畫完成
T+10.3s: .then() 回調執行
T+10.3s: reasonController.dispose() ✅
T+10.3s: 調用 cancelBookingWithSupabase()
T+10.3s: 記錄「請求 URL」
T+11s: Supabase API 處理請求
T+11.5s: API 返回成功（JSON）✅
T+11.5s: 記錄「響應狀態碼: 200」
T+11.5s: 記錄「響應 Content-Type: application/json」
T+11.5s: 顯示「訂單已取消」SnackBar ✅
T+11.5s: Provider 更新狀態
T+11.5s: UI 重新渲染
T+30s: Cron Job 執行 Edge Function
T+31s: Firestore 同步完成 ✅
```

---

## ✅ 成功標準

### 修復成功的標誌

1. **管理後台正常編譯** ⭐
   - 沒有「Module not found」錯誤
   - 顯示「Ready in ...ms」

2. **API 返回 JSON** ⭐
   - 不返回 HTML 錯誤頁面
   - Content-Type 為 application/json

3. **不出現 Flutter 框架錯誤** ⭐
   - 不出現 `_dependents.isEmpty` 錯誤
   - 對話框正常關閉

4. **取消訂單功能正常**
   - API 調用成功
   - 資料正確更新
   - UI 正確顯示

5. **資料同步正常**
   - Supabase 有資料
   - Firestore 同步完成
   - 兩邊資料一致

---

## 📚 相關文檔

1. **`docs/20251008_2047_18_取消訂單API模組導入錯誤修復.md`**
   - 管理後台 API 修復

2. **`docs/20251008_1935_16_取消訂單TextEditingController錯誤修復.md`**
   - Flutter 客戶端修復

3. **`docs/20251008_2026_17_API錯誤處理FormatException修復.md`**
   - API 錯誤處理修復（幫助診斷問題）

4. **`取消訂單功能-測試指南.md`**
   - 之前的測試指南

---

**修復狀態**：✅ 已完成  
**測試狀態**：⏳ 待執行

🚀 **請立即測試取消訂單功能！**

**最重要的測試**：
1. ✅ 重啟管理後台（確認沒有編譯錯誤）
2. ✅ 使用 curl 測試 API 端點（確認返回 JSON）
3. ✅ 重新建置 Flutter App
4. ✅ 測試完整取消訂單流程
5. ✅ 確認不出現任何錯誤
6. ✅ 確認訂單成功取消

**如果測試成功**：
- 🎉 恭喜！取消訂單功能完全修復
- 🎉 所有已知問題都已解決
- 🎉 系統功能完整且穩定
- 🎉 可以進行下一階段的開發或測試

