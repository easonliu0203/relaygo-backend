# Firestore 聊天權限錯誤修復

**日期**: 2025-10-12  
**時間**: 08:06  
**編號**: 05  
**主旨**: Firestore 聊天權限錯誤修復

---

## 📋 問題描述

### 錯誤現象

在 Flutter APP 中測試聊天功能時，客戶端和司機端都遇到了 Firestore 權限錯誤：

**客戶端錯誤**:
```
載入聊天室失敗
[cloud_firestore/permission-denied] The caller does not have permission to execute the specified operation.
```

**司機端錯誤**:
```
載入聊天室失敗
[cloud_firestore/permission-denied] The caller does not have permission to execute the specified operation.
```

**錯誤位置**: 聊天室列表頁面（`ChatListPage`）

---

## 🔍 問題診斷

### 1. 根本原因

**Firestore Security Rules 缺少 `chat_rooms` collection 的規則**

檢查 `firebase/firestore.rules` 文件後發現：
- ✅ 有 `bookings` collection 的規則
- ✅ 有 `orders_rt` collection 的規則
- ✅ 有 `users` collection 的規則
- ✅ 有 `drivers` collection 的規則
- ❌ **沒有 `chat_rooms` collection 的規則**

### 2. 查詢邏輯問題

**原始 ChatService 查詢邏輯**:
```dart
// 問題：查詢所有聊天室，然後在客戶端過濾
return _firestore
    .collection('chat_rooms')
    .orderBy('lastMessageTime', descending: true)
    .snapshots()
    .map((snapshot) {
  return snapshot.docs
      .map((doc) => ChatRoom.fromFirestore(doc))
      .where((room) {
        // 在客戶端過濾
        return room.customerId == currentUser.uid ||
            room.driverId == currentUser.uid;
      })
      .toList();
});
```

**問題**:
1. 查詢所有聊天室會觸發權限檢查
2. Firestore Security Rules 不允許讀取不屬於自己的聊天室
3. 即使在客戶端過濾，權限檢查仍然會失敗

---

## 🛠️ 修復方案

### 方案 1: 修改 Firestore Security Rules（已實施）

**添加 `chat_rooms` collection 的規則**:

```javascript
// 聊天室規則（chat_rooms 集合）
// 此集合由 Supabase Edge Function 自動同步，客戶端只能讀取
match /chat_rooms/{roomId} {
  // 允許用戶讀取自己參與的聊天室
  // 注意：使用 exists() 來允許讀取不存在的文檔（返回 null）
  // 這樣可以避免在 Edge Function 同步延遲時出現權限錯誤
  allow read: if request.auth != null &&
    (
      // 文檔不存在時允許讀取（會返回 null）
      !exists(/databases/$(database)/documents/chat_rooms/$(roomId))
      ||
      // 文檔存在時檢查是否為用戶自己的聊天室
      (resource.data.customerId == request.auth.uid ||
       resource.data.driverId == request.auth.uid)
    );

  // 禁止客戶端寫入（由 Supabase Edge Function 寫入）
  // 所有寫入操作必須通過 Supabase API
  allow write: if false;

  // 聊天訊息規則（chat_rooms/{roomId}/messages 子集合）
  match /messages/{messageId} {
    // 允許用戶讀取自己參與的聊天室的訊息
    allow read: if request.auth != null &&
      (
        // 文檔不存在時允許讀取（會返回 null）
        !exists(/databases/$(database)/documents/chat_rooms/$(roomId))
        ||
        // 文檔存在時檢查是否為用戶自己的聊天室
        (get(/databases/$(database)/documents/chat_rooms/$(roomId)).data.customerId == request.auth.uid ||
         get(/databases/$(database)/documents/chat_rooms/$(roomId)).data.driverId == request.auth.uid)
      );

    // 禁止客戶端寫入（由 Supabase Edge Function 寫入）
    // 所有寫入操作必須通過 Supabase API
    allow write: if false;
  }
}
```

### 方案 2: 修改 ChatService 查詢邏輯（已實施）

**使用兩個獨立的查詢，然後合併結果**:

```dart
/// 獲取聊天室列表（Stream）
Stream<List<ChatRoom>> getChatRoomsStream() {
  try {
    final currentUser = _firebaseService.currentUser;
    if (currentUser == null) {
      return Stream.value([]);
    }

    // 由於 Firestore 不支持 OR 查詢，我們需要創建兩個獨立的查詢
    // 一個查詢 customerId，一個查詢 driverId
    final customerRoomsStream = _firestore
        .collection('chat_rooms')
        .where('customerId', isEqualTo: currentUser.uid)
        .orderBy('lastMessageTime', descending: true)
        .snapshots()
        .map((snapshot) => snapshot.docs.map((doc) => ChatRoom.fromFirestore(doc)).toList());

    final driverRoomsStream = _firestore
        .collection('chat_rooms')
        .where('driverId', isEqualTo: currentUser.uid)
        .orderBy('lastMessageTime', descending: true)
        .snapshots()
        .map((snapshot) => snapshot.docs.map((doc) => ChatRoom.fromFirestore(doc)).toList());

    // 合併兩個 Stream
    return Rx.combineLatest2<List<ChatRoom>, List<ChatRoom>, List<ChatRoom>>(
      customerRoomsStream,
      driverRoomsStream,
      (customerRooms, driverRooms) {
        // 合併並去重（使用 bookingId）
        final allRooms = <String, ChatRoom>{};
        for (final room in customerRooms) {
          allRooms[room.bookingId] = room;
        }
        for (final room in driverRooms) {
          allRooms[room.bookingId] = room;
        }
        
        // 按最後訊息時間排序
        final sortedRooms = allRooms.values.toList()
          ..sort((a, b) {
            if (a.lastMessageTime == null && b.lastMessageTime == null) return 0;
            if (a.lastMessageTime == null) return 1;
            if (b.lastMessageTime == null) return -1;
            return b.lastMessageTime!.compareTo(a.lastMessageTime!);
          });
        
        return sortedRooms;
      },
    );
  } catch (e) {
    print('❌ 獲取聊天室列表失敗: $e');
    return Stream.value([]);
  }
}
```

### 方案 3: 添加 rxdart 依賴（已實施）

**修改 `pubspec.yaml`**:
```yaml
dependencies:
  # ... 其他依賴
  rxdart: ^0.27.7
```

---

## 📝 修改的文件

### 1. `firebase/firestore.rules`
- ✅ 添加 `chat_rooms` collection 的讀取規則
- ✅ 添加 `chat_rooms/{roomId}/messages` 子集合的讀取規則
- ✅ 使用 `exists()` 允許讀取不存在的文檔（避免同步延遲問題）
- ✅ 禁止客戶端寫入（所有寫入由 Supabase Edge Function 處理）

### 2. `mobile/lib/core/services/chat_service.dart`
- ✅ 添加 `rxdart` 導入
- ✅ 修改 `getChatRoomsStream()` 方法
- ✅ 使用兩個獨立的查詢（customerId 和 driverId）
- ✅ 使用 `Rx.combineLatest2` 合併兩個 Stream
- ✅ 去重並排序聊天室列表

### 3. `mobile/pubspec.yaml`
- ✅ 添加 `rxdart: ^0.27.7` 依賴

---

## 🚀 部署步驟

### 1. 更新依賴
```bash
cd mobile
flutter pub get
```

### 2. 重新生成 freezed 代碼
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. 部署 Firestore Security Rules
```bash
cd ../firebase
firebase deploy --only firestore:rules
```

**部署結果**:
```
=== Deploying to 'ride-platform-f1676'...

i  deploying firestore
+  cloud.firestore: rules file firestore.rules compiled successfully
+  firestore: released rules firestore.rules to cloud.firestore

+  Deploy complete!
```

---

## ✅ 驗證結果

### 預期結果

完成修復後，客戶端和司機端應該能夠：

1. ✅ 成功載入聊天室列表
2. ✅ 只看到自己參與的聊天室（customerId 或 driverId 等於當前用戶 UID）
3. ✅ 聊天室按最後訊息時間排序
4. ✅ 顯示未讀訊息數量
5. ✅ 點擊聊天室進入聊天詳情頁面
6. ✅ 成功載入聊天訊息列表

### 測試步驟

1. **重新建置並運行客戶端 APP**:
   ```bash
   cd mobile
   flutter run -t lib/apps/customer/main_customer.dart -d emulator-5554
   ```

2. **登入測試帳號**

3. **點擊底部導覽列的「聊天」選項**

4. **確認聊天室列表正常顯示**:
   - 沒有權限錯誤
   - 顯示聊天室列表（如果有訂單）
   - 或顯示空狀態（如果沒有訂單）

5. **重複測試司機端 APP**

---

## 💡 技術要點

### 1. Firestore Security Rules 設計原則

**允許讀取不存在的文檔**:
```javascript
allow read: if request.auth != null &&
  (
    !exists(/databases/$(database)/documents/chat_rooms/$(roomId))
    ||
    (resource.data.customerId == request.auth.uid ||
     resource.data.driverId == request.auth.uid)
  );
```

**原因**:
- Supabase Edge Function 同步到 Firestore 可能有延遲
- 如果文檔還不存在，查詢會返回 null 而不是權限錯誤
- 這樣可以避免在同步延遲時出現錯誤

### 2. Firestore OR 查詢限制

**問題**: Firestore 不支持在 Security Rules 中使用 OR 查詢

**解決方案**: 使用兩個獨立的查詢，然後在客戶端合併
- 查詢 1: `where('customerId', isEqualTo: currentUser.uid)`
- 查詢 2: `where('driverId', isEqualTo: currentUser.uid)`
- 使用 `Rx.combineLatest2` 合併兩個 Stream

### 3. 讀寫分離（CQRS 架構）

**寫入**: 通過 Supabase API → PostgreSQL → Edge Function → Firestore
**讀取**: 直接從 Firestore 讀取

**Security Rules 設計**:
- `allow read`: 允許已認證用戶讀取自己的資料
- `allow write: if false`: 禁止客戶端寫入，所有寫入由 Edge Function 處理

---

## 🎯 總結

### 完成的工作

- ✅ 診斷 Firestore 權限錯誤根本原因
- ✅ 添加 `chat_rooms` collection 的 Security Rules
- ✅ 修改 ChatService 查詢邏輯（使用兩個獨立查詢）
- ✅ 添加 rxdart 依賴
- ✅ 重新生成 freezed 代碼
- ✅ 部署 Firestore Security Rules

### 學到的經驗

1. **Firestore Security Rules 必須完整**: 每個 collection 都需要明確的規則
2. **OR 查詢需要特殊處理**: 使用多個查詢然後合併
3. **同步延遲需要考慮**: 使用 `exists()` 允許讀取不存在的文檔
4. **CQRS 架構的安全性**: 客戶端只能讀取，不能寫入

### 下一步

1. ⚠️ 在模擬器中測試聊天功能
2. ⚠️ 創建測試訂單並測試聊天室列表
3. ⚠️ 測試發送訊息功能
4. ⚠️ 測試未讀訊息計數
5. ⚠️ 測試已讀狀態更新

---

**修復狀態**: ✅ **完成**  
**部署狀態**: ✅ **已部署**  
**測試狀態**: ⚠️ **待測試**


