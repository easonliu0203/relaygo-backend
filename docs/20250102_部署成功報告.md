# Edge Functions 部署成功報告

**日期**：2025-10-02  
**時間**：17:20 UTC (01:20 +08:00)  
**狀態**：✅ 部署成功

---

## 🎉 部署總結

### 已完成的步驟

- ✅ **步驟 1**：資料庫 Migration（outbox 表和 trigger）
- ✅ **步驟 2**：環境變數（FIREBASE_PROJECT_ID 和 FIREBASE_API_KEY）
- ✅ **步驟 3**：部署 Edge Functions（**剛剛完成**）
- ✅ **步驟 4**：Cron Job（每 30 秒同步，每天凌晨 2 點清理）
- ✅ **步驟 5**：Firestore 規則（orders_rt 只讀）

**完成度**：100% ✅

---

## 📊 已部署的 Edge Functions

| 函數名稱 | ID | 狀態 | 版本 | 部署時間 (UTC) |
|---------|-----|------|------|----------------|
| **sync-to-firestore** | a3a887b1-d6fe-4b39-87ef-15512588455c | ✅ ACTIVE | 1 | 2025-10-02 17:20:24 |
| **cleanup-outbox** | ffecef91-d6af-474d-a93d-3ceeb1c4e40f | ✅ ACTIVE | 1 | 2025-10-02 17:21:00 |

### 函數 URL

- **sync-to-firestore**: https://vlyhwegpvpnjyocqmfqc.supabase.co/functions/v1/sync-to-firestore
- **cleanup-outbox**: https://vlyhwegpvpnjyocqmfqc.supabase.co/functions/v1/cleanup-outbox

### Dashboard 連結

- 🔗 https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc/functions

---

## 🔧 部署過程

### 執行的命令

```bash
# 1. 初始化 Supabase 配置
npx supabase init

# 2. 連接到專案
npx supabase link --project-ref vlyhwegpvpnjyocqmfqc

# 3. 部署 sync-to-firestore 函數
npx supabase functions deploy sync-to-firestore

# 4. 部署 cleanup-outbox 函數
npx supabase functions deploy cleanup-outbox

# 5. 驗證部署
npx supabase functions list
```

### 部署結果

所有命令都成功執行，沒有錯誤。

---

## 🧪 接下來的驗證步驟

### 1. 手動觸發同步測試（約 2 分鐘）

**目的**：驗證函數是否能正常執行

**步驟**：

1. 前往 Supabase SQL Editor：
   - URL: https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc/sql/new

2. 複製並執行 `supabase/test-sync-function.sql` 的內容：
   ```sql
   SELECT
     net.http_post(
       url := 'https://vlyhwegpvpnjyocqmfqc.supabase.co/functions/v1/sync-to-firestore',
       headers := jsonb_build_object(
         'Authorization', 'Bearer ' || current_setting('app.settings.service_role_key', true)
       )
     ) as request_id;
   ```

3. 應該返回一個 `request_id`（UUID 格式）

### 2. 查看函數日誌（約 1 分鐘）

**目的**：確認函數執行結果和環境變數配置

**步驟**：

1. 前往 Edge Functions 頁面：
   - URL: https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc/functions

2. 點擊 `sync-to-firestore` 函數

3. 點擊 "Logs" 分頁

4. 查看最新的日誌：

   **✅ 如果配置正確，應該看到**：
   ```
   [INFO] 找到 0 個待處理事件
   [INFO] 處理完成: 成功 0, 失敗 0
   ```
   或
   ```
   [INFO] 找到 X 個待處理事件
   [INFO] 處理事件: xxx, 類型: created, 聚合: xxx
   [INFO] Firestore 文檔已更新: orders_rt/xxx
   [INFO] 事件 xxx 處理成功
   [INFO] 處理完成: 成功 X, 失敗 0
   ```

   **❌ 如果配置錯誤，會看到**：
   ```
   [ERROR] Firestore 更新失敗: 401 Unauthorized
   ```
   或
   ```
   [ERROR] Firestore 更新失敗: 403 Forbidden
   ```

### 3. 創建測試訂單（約 5 分鐘）

**目的**：端到端測試整個同步流程

**步驟**：

1. **在應用中創建訂單**：
   ```bash
   cd d:\repo/mobile
   flutter run --flavor customer --target lib/apps/customer/main_customer.dart
   ```

2. **檢查 Supabase orders 表**：
   - 前往：https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc/editor
   - 選擇 `orders` 表
   - 確認看到新訂單

3. **檢查 Supabase outbox 表**：
   - 選擇 `outbox` 表
   - 確認看到新事件（`event_type = 'created'`）
   - `processed_at` 應該是 `NULL`（尚未處理）

4. **等待 30 秒**（Cron Job 會自動執行）

5. **重新檢查 outbox 表**：
   - 重新整理頁面
   - **✅ 如果同步成功**：`processed_at` 應該有時間戳記
   - **❌ 如果同步失敗**：`processed_at` 仍然是 `NULL`，且 `error_message` 會有錯誤訊息

6. **檢查 Firestore**：
   - 前往：https://console.firebase.google.com
   - 點擊 "Firestore Database"
   - 找到 `orders_rt` 集合
   - **✅ 如果同步成功**：應該看到新訂單文檔
   - **❌ 如果同步失敗**：集合可能不存在或沒有新文檔

7. **檢查應用顯示**：
   - 在「預約成功」頁面確認顯示訂單資訊
   - 在「我的訂單」中確認看到該訂單

---

## 📋 驗證檢查清單

請按照以下順序進行驗證：

- [ ] **步驟 1**：手動觸發同步測試
  - [ ] SQL 執行成功，返回 `request_id`
  
- [ ] **步驟 2**：查看函數日誌
  - [ ] 日誌中沒有 `401` 或 `403` 錯誤
  - [ ] 日誌顯示「處理完成」或「沒有待處理的事件」
  
- [ ] **步驟 3**：創建測試訂單
  - [ ] orders 表有新訂單
  - [ ] outbox 表有新事件
  - [ ] 等待 30 秒後，outbox 事件被處理（`processed_at` 有值）
  - [ ] Firestore `orders_rt` 集合有新文檔
  - [ ] 應用中可以看到訂單

---

## 🎯 預期的系統行為

部署完成後，系統應該實現以下行為：

### 資料流

```
App → Supabase API → orders 表 (寫入)
                        ↓
                   Trigger 監聽
                        ↓
                   outbox 表 (事件佇列)
                        ↓
              Edge Function 消費 (每 30 秒)
                        ↓
            Firestore orders_rt 集合 (鏡像)
                        ↓
                   App 讀取 (即時)
```

### 關鍵特性

- ✅ **Single Source of Truth**：Supabase 是唯一的資料寫入來源
- ✅ **單向資料流**：資料只從 Supabase 流向 Firestore
- ✅ **自動同步**：每 30 秒自動同步新訂單
- ✅ **可靠性**：失敗事件自動重試（最多 3 次）
- ✅ **自動清理**：舊事件自動清理（保留 7 天）

### 用戶體驗

- ✅ 創建訂單後，30 秒內自動同步到 Firestore
- ✅ 「預約成功」頁面正常顯示訂單資訊
- ✅ 「我的訂單」頁面正常顯示所有訂單
- ✅ 訂單狀態即時更新

---

## 🔍 監控和維護

### 每日檢查

```sql
-- 檢查未處理事件數量
SELECT COUNT(*) FROM outbox WHERE processed_at IS NULL;

-- 檢查失敗事件
SELECT * FROM outbox WHERE retry_count >= 3;
```

### 每週檢查

```sql
-- 檢查 Cron Job 執行統計
SELECT 
  jobname,
  COUNT(*) as total_runs,
  SUM(CASE WHEN status = 'succeeded' THEN 1 ELSE 0 END) as successful_runs,
  SUM(CASE WHEN status = 'failed' THEN 1 ELSE 0 END) as failed_runs
FROM cron.job_run_details
WHERE start_time > NOW() - INTERVAL '7 days'
GROUP BY jobname;
```

### 查看 Edge Function 日誌

- 前往：https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc/functions
- 點擊函數名稱 → Logs
- 定期檢查是否有錯誤

---

## 📞 如果遇到問題

### 問題 1：函數日誌顯示 401 或 403 錯誤

**原因**：Firebase API Key 配置錯誤

**解決方法**：
1. 確認 `FIREBASE_API_KEY` 是 Web API Key（`AIzaSyC...` 格式）
2. 不是 Service Account 的 `private_key`
3. 重新設置環境變數

### 問題 2：outbox 事件一直沒有被處理

**原因**：Cron Job 沒有正確設置或執行

**解決方法**：
1. 檢查 Cron Job 是否存在：
   ```sql
   SELECT * FROM cron.job;
   ```
2. 查看 Cron Job 執行歷史：
   ```sql
   SELECT * FROM cron.job_run_details 
   WHERE jobid IN (SELECT jobid FROM cron.job WHERE jobname = 'sync-orders-to-firestore')
   ORDER BY start_time DESC
   LIMIT 10;
   ```
3. 手動觸發同步（使用上面的 SQL）

### 問題 3：Firestore 沒有資料

**原因**：Project ID 錯誤或 Firestore 規則阻止寫入

**解決方法**：
1. 檢查 `FIREBASE_PROJECT_ID` 環境變數是否正確
2. 檢查 Edge Function 日誌中的錯誤訊息
3. 確認 Firestore 規則允許服務端寫入

---

## 🎊 恭喜！

您已經成功完成了 Supabase Outbox Pattern 的部署！

現在您的系統已經使用單向鏡像模式：
- ✅ Supabase 是唯一的資料寫入來源
- ✅ Firestore 作為即時資料的鏡像
- ✅ 資料自動同步（每 30 秒）
- ✅ 舊事件自動清理（每天凌晨 2 點）

**接下來請執行驗證步驟，確保一切正常運作！** 🚀

---

## 📚 相關文檔

- **[快速開始指南](../supabase/QUICK_START.md)** - 快速部署指南
- **[完整部署指南](../supabase/DEPLOYMENT_GUIDE.md)** - 詳細的部署步驟
- **[部署檢查清單](../supabase/DEPLOYMENT_CHECKLIST.md)** - 完整的檢查清單
- **[Outbox Pattern 設置](../supabase/OUTBOX_PATTERN_SETUP.md)** - 技術文檔

