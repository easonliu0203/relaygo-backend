# Firestore 雙寫策略實施

**日期**：2025-10-07 00:22  
**編號**：11  
**主旨**：實施 Firestore 雙寫策略，同時寫入 orders_rt 和 bookings 集合

---

## 📋 問題描述

### 症狀
1. ✅ 支付流程成功完成
2. ✅ 「預約成功」頁面正確顯示訂單內容
3. ✅ Supabase `bookings` 表有新訂單記錄
4. ✅ Supabase `outbox` 表有對應的事件記錄
5. ✅ Firestore `orders_rt` 集合有新訂單（資料格式正確）
6. ❌ **Firestore `bookings` 集合沒有新訂單**

### 背景資訊
- Edge Function (`sync-to-firestore`) 只寫入 `orders_rt` 集合
- 客戶端 App 從 `orders_rt` 集合讀取訂單（即時訂單）
- `bookings` 集合應該作為完整的訂單記錄（歷史記錄）
- 兩個集合應該保持同步

---

## 🔍 問題診斷過程

### 1. 檢查 Edge Function 同步邏輯

**檔案**：`supabase/functions/sync-to-firestore/index.ts`

**原始代碼**（修復前）：
```typescript
/**
 * 創建或更新 Firestore 文檔
 */
async function upsertFirestoreDocument(bookingId: string, data: any): Promise<void> {
  console.log(`準備更新 Firestore: orders_rt/${bookingId}`)

  // 獲取 Access Token
  const token = await getAccessToken()

  // 使用 Firestore REST API
  const url = `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/orders_rt/${bookingId}`
  
  // ❌ 只寫入 orders_rt 集合
  
  const response = await fetch(url, {
    method: 'PATCH',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`,
    },
    body: JSON.stringify({
      fields: firestoreFields,
    }),
  })

  if (!response.ok) {
    const errorText = await response.text()
    console.error(`Firestore 更新失敗 (${response.status}):`, errorText)
    throw new Error(`Firestore 更新失敗 (${response.status}): ${errorText}`)
  }

  console.log(`✅ Firestore 文檔已更新: orders_rt/${bookingId}`)
}
```

**發現**：
- ❌ 只寫入 `orders_rt` 集合
- ❌ 沒有寫入 `bookings` 集合
- ❌ 沒有雙寫邏輯

---

### 2. 檢查 Firestore 規則

**檔案**：`firebase/firestore.rules`

**bookings 集合規則**（第 16-25 行）：
```javascript
// 訂單規則（舊版 bookings 集合）
match /bookings/{bookingId} {
  allow read: if request.auth != null &&
    (request.auth.uid == resource.data.customerId ||
     request.auth.uid == resource.data.driverId);
  allow create: if request.auth != null &&
    request.auth.uid == request.resource.data.customerId;
  allow update: if request.auth != null &&
    (request.auth.uid == resource.data.customerId ||
     request.auth.uid == resource.data.driverId);
}
```

**orders_rt 集合規則**（第 29-36 行）：
```javascript
// 訂單即時鏡像規則（orders_rt 集合）
// 此集合由 Supabase Edge Function 自動同步，客戶端只能讀取
match /orders_rt/{orderId} {
  // 允許用戶讀取自己的訂單
  allow read: if request.auth != null
              && resource.data.customerId == request.auth.uid;

  // 禁止客戶端寫入（由 Supabase 寫入）
  allow write: if false;
}
```

**發現**：
- ✅ `bookings` 集合有規則配置
- ✅ `orders_rt` 集合有規則配置
- ✅ Service Account 可以繞過規則寫入（使用 OAuth 2.0 Token）

---

### 3. 檢查 Firestore 索引

**檔案**：`firebase/firestore.indexes.json`

**bookings 集合索引**（已存在）：
```json
{
  "collectionGroup": "bookings",
  "queryScope": "COLLECTION",
  "fields": [
    { "fieldPath": "customerId", "order": "ASCENDING" },
    { "fieldPath": "createdAt", "order": "DESCENDING" }
  ]
}
```

**orders_rt 集合索引**（修復前缺失）：
- ❌ 沒有 `orders_rt` 集合的索引配置

---

### 4. 根本原因

**問題**：
- Edge Function 只實現了單寫邏輯（只寫入 `orders_rt`）
- 沒有實現雙寫邏輯（同時寫入 `orders_rt` 和 `bookings`）
- `orders_rt` 集合缺少索引配置

**為什麼需要雙寫？**
- `orders_rt`：客戶端即時訂單（用於 App 顯示）
- `bookings`：完整訂單記錄（用於歷史查詢、報表、管理後台）
- 兩個集合應該保持同步

---

## 🔧 修復方案：雙寫策略

### 策略說明

**雙寫策略**：
- 每次訂單變更時，同時寫入 `orders_rt` 和 `bookings` 兩個集合
- 確保資料格式一致
- 提供容錯機制（部分失敗不影響整體）

**優點**：
- ✅ 資料冗餘，提高可靠性
- ✅ 分離關注點（即時訂單 vs 歷史記錄）
- ✅ 提高查詢效能（不同用途使用不同集合）

**缺點**：
- ⚠️ 增加寫入成本
- ⚠️ 可能出現短暫的資料不一致（如果一個成功一個失敗）

---

## 🛠️ 修復實施

### 修改 1：更新 `upsertFirestoreDocument` 函數

**檔案**：`supabase/functions/sync-to-firestore/index.ts`

#### 修復前（錯誤）❌

```typescript
async function upsertFirestoreDocument(bookingId: string, data: any): Promise<void> {
  console.log(`準備更新 Firestore: orders_rt/${bookingId}`)

  const token = await getAccessToken()
  const url = `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/orders_rt/${bookingId}`
  
  // ❌ 只寫入 orders_rt
  
  const response = await fetch(url, { ... })
  
  if (!response.ok) {
    throw new Error(`Firestore 更新失敗 (${response.status}): ${errorText}`)
  }

  console.log(`✅ Firestore 文檔已更新: orders_rt/${bookingId}`)
}
```

---

#### 修復後（正確）✅

```typescript
/**
 * 創建或更新 Firestore 文檔（雙寫策略：同時寫入 orders_rt 和 bookings）
 */
async function upsertFirestoreDocument(bookingId: string, data: any): Promise<void> {
  console.log(`準備更新 Firestore（雙寫）: orders_rt/${bookingId} 和 bookings/${bookingId}`)

  // 獲取 Access Token
  const token = await getAccessToken()

  // 轉換為 Firestore 格式
  const firestoreFields = convertToFirestoreFields(data)

  // ✅ 定義兩個集合的 URL
  const collections = [
    { name: 'orders_rt', url: `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/orders_rt/${bookingId}` },
    { name: 'bookings', url: `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/bookings/${bookingId}` },
  ]

  // 記錄成功和失敗的集合
  const results = {
    success: [] as string[],
    failed: [] as { collection: string; error: string }[],
  }

  // ✅ 依次寫入兩個集合
  for (const collection of collections) {
    try {
      const response = await fetch(collection.url, {
        method: 'PATCH',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${token}`,
        },
        body: JSON.stringify({
          fields: firestoreFields,
        }),
      })

      if (!response.ok) {
        const errorText = await response.text()
        console.error(`Firestore 更新失敗 (${collection.name}, ${response.status}):`, errorText)
        results.failed.push({
          collection: collection.name,
          error: `${response.status}: ${errorText}`,
        })
      } else {
        console.log(`✅ Firestore 文檔已更新: ${collection.name}/${bookingId}`)
        results.success.push(collection.name)
      }
    } catch (error) {
      console.error(`Firestore 更新異常 (${collection.name}):`, error)
      results.failed.push({
        collection: collection.name,
        error: error.message,
      })
    }
  }

  // ✅ 檢查結果並提供容錯機制
  if (results.failed.length > 0) {
    const errorMsg = `部分集合更新失敗: ${results.failed.map(f => `${f.collection} (${f.error})`).join(', ')}`
    console.error(errorMsg)
    
    // 如果兩個都失敗，拋出錯誤
    if (results.success.length === 0) {
      throw new Error(`所有集合更新失敗: ${errorMsg}`)
    }
    
    // 如果只有一個失敗，記錄警告但不拋出錯誤
    console.warn(`⚠️ 雙寫部分成功: 成功 [${results.success.join(', ')}], 失敗 [${results.failed.map(f => f.collection).join(', ')}]`)
  } else {
    console.log(`✅ 雙寫成功: orders_rt/${bookingId} 和 bookings/${bookingId}`)
  }
}
```

**關鍵變更**：
1. ✅ 定義兩個集合的 URL
2. ✅ 依次寫入兩個集合
3. ✅ 記錄每個集合的成功/失敗狀態
4. ✅ 提供容錯機制：
   - 如果兩個都失敗 → 拋出錯誤
   - 如果只有一個失敗 → 記錄警告但不拋出錯誤
   - 如果兩個都成功 → 記錄成功

---

### 修改 2：更新 `deleteFirestoreDocument` 函數

**檔案**：`supabase/functions/sync-to-firestore/index.ts`

#### 修復前（錯誤）❌

```typescript
async function deleteFirestoreDocument(bookingId: string): Promise<void> {
  console.log(`準備刪除 Firestore: orders_rt/${bookingId}`)

  const token = await getAccessToken()
  const url = `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/orders_rt/${bookingId}`
  
  // ❌ 只刪除 orders_rt
  
  const response = await fetch(url, { method: 'DELETE', ... })
  
  if (!response.ok && response.status !== 404) {
    throw new Error(`Firestore 刪除失敗 (${response.status}): ${errorText}`)
  }

  console.log(`✅ Firestore 文檔已刪除: orders_rt/${bookingId}`)
}
```

---

#### 修復後（正確）✅

```typescript
/**
 * 刪除 Firestore 文檔（雙刪策略：同時刪除 orders_rt 和 bookings）
 */
async function deleteFirestoreDocument(bookingId: string): Promise<void> {
  console.log(`準備刪除 Firestore（雙刪）: orders_rt/${bookingId} 和 bookings/${bookingId}`)

  const token = await getAccessToken()

  // ✅ 定義兩個集合的 URL
  const collections = [
    { name: 'orders_rt', url: `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/orders_rt/${bookingId}` },
    { name: 'bookings', url: `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT_ID}/databases/(default)/documents/bookings/${bookingId}` },
  ]

  const results = {
    success: [] as string[],
    failed: [] as { collection: string; error: string }[],
  }

  // ✅ 依次刪除兩個集合
  for (const collection of collections) {
    try {
      const response = await fetch(collection.url, {
        method: 'DELETE',
        headers: {
          'Authorization': `Bearer ${token}`,
        },
      })

      if (!response.ok && response.status !== 404) {
        const errorText = await response.text()
        console.error(`Firestore 刪除失敗 (${collection.name}, ${response.status}):`, errorText)
        results.failed.push({
          collection: collection.name,
          error: `${response.status}: ${errorText}`,
        })
      } else {
        console.log(`✅ Firestore 文檔已刪除: ${collection.name}/${bookingId}`)
        results.success.push(collection.name)
      }
    } catch (error) {
      console.error(`Firestore 刪除異常 (${collection.name}):`, error)
      results.failed.push({
        collection: collection.name,
        error: error.message,
      })
    }
  }

  // ✅ 檢查結果
  if (results.failed.length > 0) {
    const errorMsg = `部分集合刪除失敗: ${results.failed.map(f => `${f.collection} (${f.error})`).join(', ')}`
    console.error(errorMsg)
    
    if (results.success.length === 0) {
      throw new Error(`所有集合刪除失敗: ${errorMsg}`)
    }
    
    console.warn(`⚠️ 雙刪部分成功: 成功 [${results.success.join(', ')}], 失敗 [${results.failed.map(f => f.collection).join(', ')}]`)
  } else {
    console.log(`✅ 雙刪成功: orders_rt/${bookingId} 和 bookings/${bookingId}`)
  }
}
```

---

### 修改 3：添加 orders_rt 集合索引

**檔案**：`firebase/firestore.indexes.json`

#### 修復前（缺失）❌

```json
{
  "indexes": [
    // bookings 集合索引（已存在）
    { ... },
    
    // ❌ 缺少 orders_rt 集合索引
    
    // driver_locations 集合索引
    { ... }
  ]
}
```

---

#### 修復後（正確）✅

```json
{
  "indexes": [
    // bookings 集合索引
    { ... },
    
    // ✅ 新增 orders_rt 集合索引
    {
      "collectionGroup": "orders_rt",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "customerId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "orders_rt",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    
    // driver_locations 集合索引
    { ... }
  ]
}
```

---

### 修改 4：更新文件註釋

**檔案**：`supabase/functions/sync-to-firestore/index.ts`

#### 修復前（錯誤）❌

```typescript
/**
 * 資料流：
 * 1. Supabase Trigger 監聽 bookings 表變更
 * 2. 寫入 outbox 表（事件佇列）
 * 3. 本 Edge Function 消費 outbox 事件
 * 4. 推送到 Firestore orders_rt/{bookingId} 集合  // ❌ 只提到 orders_rt
 * 5. 標記事件為已處理
 */
```

---

#### 修復後（正確）✅

```typescript
/**
 * 資料流：
 * 1. Supabase Trigger 監聽 bookings 表變更
 * 2. 寫入 outbox 表（事件佇列）
 * 3. 本 Edge Function 消費 outbox 事件
 * 4. 推送到 Firestore（雙寫策略）：  // ✅ 說明雙寫策略
 *    - orders_rt/{bookingId} 集合（客戶端即時訂單）
 *    - bookings/{bookingId} 集合（完整訂單記錄）
 * 5. 標記事件為已處理
 *
 * 修復歷史：
 * - 2025-10-06: 實施雙寫策略（同時寫入 orders_rt 和 bookings）
 */
```

---

## 🚀 部署和測試

### 1. 部署 Edge Function

```bash
npx supabase functions deploy sync-to-firestore --project-ref vlyhwegpvpnjyocqmfqc
```

**結果**：✅ 部署成功

---

### 2. 測試步驟

#### 步驟 1：手動觸發 Edge Function（1 分鐘）

1. 打開：https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc/functions
2. 點擊 sync-to-firestore → Invoke
3. 預期結果：`success: 7, failure: 0`

---

#### 步驟 2：檢查 Edge Function 日誌（1 分鐘）

1. 點擊「Logs」標籤
2. 查找以下訊息：

**應該看到**：
```
準備更新 Firestore（雙寫）: orders_rt/{bookingId} 和 bookings/{bookingId}
✅ Firestore 文檔已更新: orders_rt/{bookingId}
✅ Firestore 文檔已更新: bookings/{bookingId}
✅ 雙寫成功: orders_rt/{bookingId} 和 bookings/{bookingId}
```

---

#### 步驟 3：檢查 Firestore 資料（2 分鐘）⭐ 最重要

1. **打開 Firebase Console**
   ```
   https://console.firebase.google.com
   ```

2. **選擇專案 → Firestore Database**

3. **檢查 `orders_rt` 集合**
   - 應該有新訂單
   - 資料格式正確（GeoPoint、Timestamp、Integer）

4. **檢查 `bookings` 集合**⭐ 重點
   - ✅ 應該有新訂單（與 `orders_rt` 相同的訂單）
   - ✅ 資料格式一致
   - ✅ 文檔 ID 相同

---

#### 步驟 4：測試客戶端 App（1 分鐘）

1. 重新啟動 App
2. 查看訂單列表
3. 確認訂單顯示正常

---

### 3. 驗證成功的標準

**Edge Function 日誌**：
```
✅ 準備更新 Firestore（雙寫）
✅ Firestore 文檔已更新: orders_rt/{bookingId}
✅ Firestore 文檔已更新: bookings/{bookingId}
✅ 雙寫成功
```

**Firestore 資料**：
```
orders_rt 集合:
  - {bookingId}: { customerId, status, pickupLocation, ... }  ✅

bookings 集合:
  - {bookingId}: { customerId, status, pickupLocation, ... }  ✅ 新增

兩個集合的資料應該完全一致
```

**客戶端 App**：
```
✅ 訂單列表顯示正常
✅ 訂單詳情顯示正常
✅ 不顯示任何錯誤
```

---

## 💡 開發心得和經驗總結

### 1. 雙寫策略的優缺點

**優點**：
- ✅ 資料冗餘，提高可靠性
- ✅ 分離關注點（即時訂單 vs 歷史記錄）
- ✅ 提高查詢效能（不同用途使用不同集合）
- ✅ 容易實施（不需要複雜的同步機制）

**缺點**：
- ⚠️ 增加寫入成本（每次寫入兩次）
- ⚠️ 可能出現短暫的資料不一致（如果一個成功一個失敗）
- ⚠️ 需要維護兩個集合的索引和規則

---

### 2. 容錯機制的重要性

**為什麼需要容錯？**
- 網路可能不穩定
- Firestore 可能暫時不可用
- 權限配置可能有問題

**容錯策略**：
- 如果兩個都失敗 → 拋出錯誤，觸發重試
- 如果只有一個失敗 → 記錄警告，但不影響整體流程
- 記錄詳細的錯誤訊息，方便診斷

---

### 3. 日誌記錄的價值

**詳細的日誌**：
```
準備更新 Firestore（雙寫）: orders_rt/{bookingId} 和 bookings/{bookingId}
✅ Firestore 文檔已更新: orders_rt/{bookingId}
✅ Firestore 文檔已更新: bookings/{bookingId}
✅ 雙寫成功: orders_rt/{bookingId} 和 bookings/{bookingId}
```

**幫助診斷**：
- 可以快速確認哪個集合寫入成功
- 可以快速確認哪個集合寫入失敗
- 可以追蹤整個雙寫流程

---

### 4. 遇到的困難

**困難 1：如何處理部分失敗**
- 如果 `orders_rt` 成功但 `bookings` 失敗，應該怎麼辦？
- 解決：記錄警告但不拋出錯誤，允許部分成功

**困難 2：如何確保資料一致性**
- 兩個集合的資料格式必須完全一致
- 解決：使用相同的 `convertToFirestoreFields` 函數

**困難 3：如何測試雙寫**
- 需要同時檢查兩個集合
- 解決：提供詳細的測試步驟和驗證標準

---

## 📋 後續改進

### 1. 監控雙寫狀態

**建議**：添加監控指標
- 雙寫成功率
- 部分失敗率
- 完全失敗率

**實施**：
- 在 Edge Function 中添加計數器
- 定期檢查日誌
- 設置告警

---

### 2. 補寫歷史資料

**問題**：修復前的訂單只在 `orders_rt` 集合中

**解決方案**：
1. 查詢 `orders_rt` 集合中的所有訂單
2. 檢查 `bookings` 集合中是否存在
3. 如果不存在，複製到 `bookings` 集合

**腳本示例**：
```typescript
// 補寫歷史資料腳本（待實施）
async function backfillBookings() {
  const ordersRt = await firestore.collection('orders_rt').get()
  
  for (const doc of ordersRt.docs) {
    const bookingId = doc.id
    const data = doc.data()
    
    // 檢查 bookings 集合中是否存在
    const bookingDoc = await firestore.collection('bookings').doc(bookingId).get()
    
    if (!bookingDoc.exists) {
      // 複製到 bookings 集合
      await firestore.collection('bookings').doc(bookingId).set(data)
      console.log(`✅ 補寫訂單: ${bookingId}`)
    }
  }
}
```

---

### 3. 考慮使用 Firestore Batch Write

**優點**：
- 原子性操作（全部成功或全部失敗）
- 減少網路請求次數

**缺點**：
- Firestore REST API 不支持 Batch Write
- 需要使用 Firebase Admin SDK

**未來改進**：
- 考慮將 Edge Function 改為使用 Firebase Admin SDK
- 使用 Batch Write 實現真正的事務性雙寫

---

## 📚 相關文檔

- `docs/20251006_2254_10_BookingStatus_color屬性錯誤修復.md` - BookingStatus color 修復
- `docs/20251006_2158_09_整數類型修復.md` - 整數類型修復
- `docs/20251006_2047_08_Timestamp格式修復.md` - Timestamp 修復
- `docs/20251006_0840_07_GeoPoint格式修復.md` - GeoPoint 修復
- `修復總結-所有問題已解決.md` - 所有修復總結

---

**修復狀態**：✅ 完成並部署  
**測試狀態**：⏳ 待用戶驗證  
**影響範圍**：Firestore 同步邏輯

🚀 **請立即執行測試步驟，確認 `bookings` 集合有新訂單！**

