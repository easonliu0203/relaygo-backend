# CQRS 架構修復第二階段完成報告

**日期**：2025-10-08 00:31  
**階段**：第二階段（優先級 2）  
**狀態**：✅ 代碼完成，待部署和測試

---

## 📋 執行摘要

### ✅ 已完成的工作

#### 1. 創建取消訂單 Supabase API

**檔案**：`web-admin/src/app/api/bookings/[id]/cancel/route.ts`（新增）

**API 規格**：
```
POST /api/bookings/:id/cancel
Content-Type: application/json

Request:
{
  "customerUid": "hUu4fH5dTlW9VUYm6GojXvRLdni2",
  "reason": "行程有變，需要取消"
}

Response:
{
  "success": true,
  "message": "訂單已成功取消",
  "data": {
    "id": "...",
    "status": "cancelled",
    "cancelledAt": "2025-10-07T15:30:00Z",
    "cancellationReason": "行程有變，需要取消"
  }
}
```

**功能**：
1. ✅ 驗證用戶權限（只能取消自己的訂單）
2. ✅ 檢查訂單狀態（只能取消 pending 或 matched 狀態）
3. ✅ 驗證取消原因長度（5-200 字元）
4. ✅ 更新 Supabase `bookings` 表
5. ✅ Trigger 自動寫入 `outbox` 表
6. ✅ Edge Function 自動同步到 Firestore

---

#### 2. 實現客戶端方法

**檔案**：`mobile/lib/core/services/booking_service.dart`

**新增方法**：`cancelBookingWithSupabase()`（第 164-211 行）

```dart
/// 取消訂單（使用 Supabase API）
Future<Map<String, dynamic>> cancelBookingWithSupabase(
  String bookingId,
  String reason,
) async {
  try {
    final user = _auth.currentUser;
    if (user == null) {
      throw Exception('用戶未登入');
    }

    debugPrint('[BookingService] 開始取消訂單: $bookingId');

    final requestBody = {
      'customerUid': user.uid,
      'reason': reason,
    };

    final response = await http.post(
      Uri.parse('$_baseUrl/bookings/$bookingId/cancel'),
      headers: {
        'Content-Type': 'application/json',
      },
      body: json.encode(requestBody),
    );

    if (response.statusCode == 200) {
      final data = json.decode(response.body);
      if (data['success'] == true) {
        debugPrint('[BookingService] 取消成功');

        // 資料將由 Supabase Trigger 自動鏡像到 Firestore
        // 不再從客戶端直接寫入 Firebase

        return data['data'];
      } else {
        throw Exception(data['error'] ?? '取消訂單失敗');
      }
    } else {
      final errorData = json.decode(response.body);
      throw Exception(errorData['error'] ?? '取消訂單失敗');
    }
  } catch (e) {
    debugPrint('[BookingService] 取消訂單失敗: $e');
    throw Exception('取消訂單失敗: $e');
  }
}
```

**舊版方法**：已標記為 `@Deprecated`（第 213-228 行）

```dart
@Deprecated('請使用 cancelBookingWithSupabase 方法')
Future<void> cancelBooking(String orderId) async {
  throw Exception(
    '此方法已棄用，違反 CQRS 架構原則。\n'
    '請使用 cancelBookingWithSupabase() 方法。\n'
    '所有寫入操作必須通過 Supabase API。'
  );
}
```

---

#### 3. 更新 Provider 方法

**檔案**：`mobile/lib/shared/providers/booking_provider.dart`

**新增方法**：`cancelBookingWithSupabase()`（第 138-156 行）

```dart
/// 取消預約（使用 Supabase API）
Future<void> cancelBookingWithSupabase(String bookingId, String reason) async {
  state = const BookingStateLoading();
  
  try {
    await _bookingService.cancelBookingWithSupabase(bookingId, reason);
    
    // 更新當前狀態中的訂單（如果存在）
    final currentState = state;
    if (currentState is BookingStateSuccess) {
      final updatedOrder = currentState.order.copyWith(
        status: BookingStatus.cancelled,
      );
      state = BookingStateSuccess(updatedOrder);
    }
  } catch (e) {
    state = BookingStateError(e.toString());
  }
}
```

**舊版方法**：已標記為 `@Deprecated`（第 158-179 行）

---

#### 4. 更新 UI 調用

**檔案**：`mobile/lib/apps/customer/presentation/pages/order_detail_page.dart`

**修改內容**：
- ✅ 添加取消原因輸入框（TextField）
- ✅ 驗證取消原因長度（5-200 字元）
- ✅ 調用 `cancelBookingWithSupabase()` 方法
- ✅ 顯示成功/失敗訊息

**修改前**（錯誤）：
```dart
void _showCancelDialog(...) {
  showDialog(
    ...
    content: const Text('確定要取消此訂單嗎？'),
    ...
    onPressed: () async {
      await ref.read(bookingStateProvider.notifier).cancelBooking(order.id);
    },
  );
}
```

**修改後**（正確）：
```dart
void _showCancelDialog(...) {
  final reasonController = TextEditingController();
  
  showDialog(
    ...
    content: Column(
      children: [
        const Text('確定要取消此訂單嗎？'),
        TextField(
          controller: reasonController,
          decoration: const InputDecoration(
            labelText: '取消原因',
            hintText: '請輸入取消原因（至少 5 個字元）',
          ),
          maxLines: 3,
          maxLength: 200,
        ),
      ],
    ),
    ...
    onPressed: () async {
      final reason = reasonController.text.trim();
      
      if (reason.length < 5) {
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(content: Text('取消原因至少需要 5 個字元')),
        );
        return;
      }
      
      await ref.read(bookingStateProvider.notifier).cancelBookingWithSupabase(
        order.id,
        reason,
      );
    },
  );
}
```

---

#### 5. 創建資料庫 Migration

**檔案**：`supabase/migrations/20250102_add_cancellation_fields.sql`（新增）

**新增欄位**：
```sql
-- 添加取消原因欄位
ALTER TABLE bookings
ADD COLUMN IF NOT EXISTS cancellation_reason TEXT;

-- 添加取消時間欄位
ALTER TABLE bookings
ADD COLUMN IF NOT EXISTS cancelled_at TIMESTAMP WITH TIME ZONE;

-- 創建索引
CREATE INDEX IF NOT EXISTS idx_bookings_cancelled_at 
ON bookings(cancelled_at) 
WHERE cancelled_at IS NOT NULL;
```

---

#### 6. 創建測試指南文檔

**檔案**：`CQRS修復-取消訂單功能測試指南.md`

**內容**：
- ✅ 完整的 API 測試步驟
- ✅ 資料庫驗證方法
- ✅ Firestore 同步驗證
- ✅ 客戶端 App 測試流程
- ✅ 故障排除指南
- ✅ 測試檢查清單

---

## 🎯 修復效果

### 修復前的問題 ❌

1. **取消訂單功能不可用**
   - `cancelBooking()` 方法拋出異常
   - 客戶端無法取消訂單
   - 違反 CQRS 原則

2. **缺少後端 API**
   - 沒有 Supabase API 支持
   - 無法通過正確的流程取消訂單

3. **資料庫欄位缺失**
   - 沒有 `cancellation_reason` 欄位
   - 沒有 `cancelled_at` 欄位
   - 無法記錄取消原因和時間

---

### 修復後的改進 ✅

1. **取消訂單功能完整**
   - ✅ `cancelBookingWithSupabase()` 方法可用
   - ✅ 客戶端可以正常取消訂單
   - ✅ 符合 CQRS 原則

2. **後端 API 完整**
   - ✅ POST /api/bookings/:id/cancel
   - ✅ 驗證用戶權限
   - ✅ 檢查訂單狀態
   - ✅ 驗證取消原因

3. **資料庫欄位完整**
   - ✅ `cancellation_reason` 欄位
   - ✅ `cancelled_at` 欄位
   - ✅ 可以記錄取消原因和時間

4. **UI 體驗改進**
   - ✅ 取消原因輸入框
   - ✅ 輸入驗證
   - ✅ 明確的錯誤訊息

---

## 📊 修改統計

| 項目 | 修改前 | 修改後 | 狀態 |
|------|--------|--------|------|
| **後端 API** | ❌ 不存在 | ✅ 已實現 | ✅ 完成 |
| **客戶端方法** | ❌ 拋出異常 | ✅ 可用 | ✅ 完成 |
| **Provider 方法** | ❌ 不存在 | ✅ 已實現 | ✅ 完成 |
| **UI 調用** | ❌ 舊版方法 | ✅ 新版方法 | ✅ 完成 |
| **資料庫欄位** | ❌ 缺失 | ✅ 已添加 | ✅ 完成 |
| **測試文檔** | ❌ 不存在 | ✅ 已創建 | ✅ 完成 |

---

## ⏳ 待執行的步驟

### 1. 執行資料庫 Migration（必須）

**方法 A：使用 Supabase CLI**
```bash
cd d:/repo
supabase db push
```

**方法 B：使用 Supabase Dashboard**
1. 前往：https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc/editor
2. 點擊「SQL Editor」
3. 複製 `supabase/migrations/20250102_add_cancellation_fields.sql` 內容
4. 執行 SQL

**驗證**：
```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'bookings' 
  AND column_name IN ('cancellation_reason', 'cancelled_at');
```

---

### 2. 啟動管理後台（必須）

```bash
cd web-admin
npm install  # 如果尚未安裝
npm run dev
```

**驗證**：
- 打開：http://localhost:3001
- 確認伺服器正常運行

---

### 3. 測試 API 端點（必須）

**使用 curl 測試**：
```bash
curl -X POST http://localhost:3001/api/bookings/{booking-id}/cancel \
  -H "Content-Type: application/json" \
  -d '{
    "customerUid": "hUu4fH5dTlW9VUYm6GojXvRLdni2",
    "reason": "測試取消功能"
  }'
```

**預期結果**：
```json
{
  "success": true,
  "message": "訂單已成功取消",
  "data": {...}
}
```

---

### 4. 測試客戶端 App（必須）

**步驟**：
1. 重新建置並運行 App
2. 創建測試訂單
3. 進入訂單詳情頁面
4. 點擊「取消訂單」
5. 輸入取消原因
6. 確認取消

**預期結果**：
- ✅ 顯示取消原因輸入框
- ✅ 驗證取消原因長度
- ✅ 取消成功後顯示訊息
- ✅ 訂單狀態變為「已取消」

---

### 5. 驗證資料同步（必須）

**步驟**：
1. 檢查 Supabase `bookings` 表
2. 檢查 Supabase `outbox` 表
3. 等待 30 秒（Cron Job 執行）
4. 檢查 Firestore `orders_rt` 集合
5. 檢查 Firestore `bookings` 集合

**預期結果**：
- ✅ Supabase 有取消記錄
- ✅ Outbox 有更新事件
- ✅ Firestore 狀態已同步

---

## 🚨 注意事項

### 1. 資料庫 Migration 必須先執行

**重要**：
- 必須先執行 migration 添加欄位
- 否則 API 會因為欄位不存在而失敗

**檢查方法**：
```sql
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'bookings' 
  AND column_name IN ('cancellation_reason', 'cancelled_at');
```

---

### 2. 管理後台必須運行

**重要**：
- API 端點在管理後台中
- 必須啟動 `npm run dev`
- 預設端口：3001

**檢查方法**：
- 打開：http://localhost:3001
- 應該看到管理後台頁面

---

### 3. 取消原因驗證

**規則**：
- 最少 5 個字元
- 最多 200 個字元
- 必填欄位

**客戶端驗證**：
- UI 層面驗證（即時反饋）
- API 層面驗證（安全保障）

---

### 4. 訂單狀態限制

**可取消的狀態**：
- `pending` - 待配對
- `matched` - 已配對

**不可取消的狀態**：
- `in_progress` - 進行中
- `completed` - 已完成
- `cancelled` - 已取消

---

## 💡 開發心得

### 1. CQRS 架構的完整性

**教訓**：
- 必須為所有寫入操作提供 Supabase API
- 不能留下任何直接寫入 Firestore 的方法
- 即使是「簡單」的操作也要遵循架構原則

**改進**：
- 實現完整的取消訂單 API
- 包含所有必要的驗證和檢查
- 確保資料一致性

---

### 2. 用戶體驗的重要性

**經驗**：
- 取消原因不只是技術需求
- 也是業務分析的重要資料
- 需要友好的 UI 引導用戶輸入

**實現**：
- 添加取消原因輸入框
- 提供明確的提示文字
- 即時驗證輸入長度

---

### 3. 資料庫設計的前瞻性

**問題**：
- 初始設計缺少取消相關欄位
- 需要後續添加 migration

**建議**：
- 設計時考慮完整的業務流程
- 預留可能需要的欄位
- 使用 migration 管理變更

---

### 4. 測試文檔的價值

**經驗**：
- 詳細的測試指南可以節省時間
- 包含所有可能的測試場景
- 提供故障排除方法

**實現**：
- 創建完整的測試指南
- 包含 API、資料庫、Firestore、客戶端測試
- 提供檢查清單

---

## 🔍 遇到的困難和解決方法

### 困難 1：資料庫欄位缺失

**問題**：
- 初始 schema 沒有取消相關欄位
- 需要添加 migration

**解決**：
- 創建新的 migration 文件
- 使用 `ALTER TABLE` 添加欄位
- 添加索引提高查詢效能

---

### 困難 2：UI 需要收集取消原因

**問題**：
- 原有的取消對話框只有確認按鈕
- 需要添加輸入框

**解決**：
- 修改對話框內容為 Column
- 添加 TextField 收集取消原因
- 添加客戶端驗證

---

### 困難 3：API 權限驗證

**問題**：
- 需要確保用戶只能取消自己的訂單
- 需要檢查訂單狀態

**解決**：
- 查詢訂單時 JOIN users 表
- 比對 `firebase_uid` 和請求中的 `customerUid`
- 檢查訂單狀態是否允許取消

---

## ✅ 第二階段完成檢查清單

- [x] **創建後端 API**
  - [x] 實現 POST /api/bookings/:id/cancel
  - [x] 驗證用戶權限
  - [x] 檢查訂單狀態
  - [x] 驗證取消原因

- [x] **實現客戶端方法**
  - [x] `cancelBookingWithSupabase()` 方法
  - [x] 標記舊版方法為 `@Deprecated`

- [x] **更新 Provider**
  - [x] `cancelBookingWithSupabase()` 方法
  - [x] 標記舊版方法為 `@Deprecated`

- [x] **更新 UI**
  - [x] 添加取消原因輸入框
  - [x] 驗證取消原因長度
  - [x] 調用新版方法

- [x] **創建資料庫 Migration**
  - [x] 添加 `cancellation_reason` 欄位
  - [x] 添加 `cancelled_at` 欄位
  - [x] 創建索引

- [x] **創建測試文檔**
  - [x] API 測試步驟
  - [x] 資料庫驗證
  - [x] Firestore 同步驗證
  - [x] 客戶端測試流程

- [ ] **執行 Migration**（待執行）
  - [ ] 使用 Supabase CLI 或 Dashboard
  - [ ] 驗證欄位已添加

- [ ] **測試 API**（待執行）
  - [ ] 成功取消訂單
  - [ ] 取消原因太短
  - [ ] 權限不足
  - [ ] 訂單狀態不允許取消

- [ ] **測試客戶端**（待執行）
  - [ ] 取消原因輸入
  - [ ] 取消成功
  - [ ] 訂單狀態更新

- [ ] **驗證資料同步**（待執行）
  - [ ] Supabase 資料
  - [ ] Outbox 事件
  - [ ] Firestore 同步

---

## 📚 相關文檔

1. **`docs/20251007_0847_12_CQRS架構審查報告.md`**
   - 完整的架構審查報告

2. **`CQRS架構修復計劃.md`**
   - 詳細的修復步驟

3. **`docs/20251007_2305_13_CQRS架構修復第一階段完成.md`**
   - 第一階段修復報告

4. **`CQRS修復-取消訂單功能測試指南.md`**
   - 取消訂單功能測試指南

5. **`web-admin/src/app/api/bookings/[id]/cancel/route.ts`**
   - 取消訂單 API 實現

6. **`mobile/lib/core/services/booking_service.dart`**
   - 客戶端服務實現

---

**第二階段狀態**：✅ 代碼完成，⏳ 待部署和測試  
**預計完成時間**：30-45 分鐘（部署和測試）  
**風險等級**：低（可以隨時回滾）

🚀 **請按照「CQRS修復-取消訂單功能測試指南.md」執行部署和測試！**

