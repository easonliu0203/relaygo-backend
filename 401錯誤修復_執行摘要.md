# 401 錯誤修復 - 執行摘要

> **問題**: Supabase 即時同步返回 401 Unauthorized  
> **修復時間**: 5 分鐘  
> **難度**: ⭐ 簡單（只需複製粘貼）

---

## 🎯 最快速的修復方法（推薦）

### 步驟 1: 獲取 Service Role Key（2 分鐘）

1. 打開 [Supabase Dashboard](https://supabase.com/dashboard)
2. 選擇項目 `vlyhwegpvpnjyocqmfqc`
3. 前往 **Settings** → **API**
4. 找到 **Project API keys** 區塊
5. 在 **service_role** 那一行，點擊 **Reveal** 按鈕
6. 複製整個 key（以 `eyJ` 開頭的長字符串）

**範例**:
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZseWh3ZWdwdnBuanlvY3FtZnFjIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1ODk3Nzk5NiwiZXhwIjoyMDc0NTUzOTk2fQ.nQPynfQcSIZ1QPVSjDcgscugQcEgfRPUauW0psSRTQo
```

---

### 步驟 2: 執行快速修復腳本（3 分鐘）

1. **打開文件**: `d:\repo\supabase\QUICK_FIX_401.sql`

2. **替換 Service Role Key**:
   - 找到第 15 行：`ALTER DATABASE postgres SET app.settings.service_role_key TO 'YOUR_SERVICE_ROLE_KEY';`
   - 將 `'YOUR_SERVICE_ROLE_KEY'` 替換為您剛才複製的 key
   - 保持單引號 `'...'`

   **範例**:
   ```sql
   ALTER DATABASE postgres SET app.settings.service_role_key TO 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
   ```

3. **在 Supabase SQL Editor 中執行**:
   - 前往 [Supabase Dashboard](https://supabase.com/dashboard) → SQL Editor
   - 複製整個 `QUICK_FIX_401.sql` 文件的內容
   - 粘貼到 SQL Editor
   - 點擊 **Run** 按鈕

4. **重新連接數據庫**:
   - 點擊 SQL Editor 右上角的 **Disconnect**
   - 然後點擊 **Connect** 重新連接
   - 或者直接刷新頁面

5. **再次執行測試部分**:
   - 複製腳本中「以下是測試腳本（重新連接後執行）」之後的部分
   - 在 SQL Editor 中執行

---

### 步驟 3: 驗證修復（1 分鐘）

**查看執行結果**:

1. **檢查 HTTP 請求記錄**:
   - 應該看到 `status_code = 200`（不是 401）
   - 狀態顯示：`✅ 成功 - 修復完成！`

2. **檢查 Outbox 事件**:
   - 應該看到 `status = ✅ 已處理`

3. **檢查 Firestore**:
   - 前往 [Firebase Console](https://console.firebase.google.com/project/ride-platform-f1676/firestore)
   - 打開 `orders_rt` 集合
   - 應該看到剛才創建的測試訂單

---

## ✅ 修復成功的標誌

### HTTP 請求記錄

**修復前** ❌:
```
status_code: 401
status: ❌ 認證失敗
```

**修復後** ✅:
```
status_code: 200
status: ✅ 成功 - 修復完成！
```

---

### Outbox 事件

**修復前** ❌:
```
status: ⏳ 待處理
processed_at: NULL
retry_count: 0-3
```

**修復後** ✅:
```
status: ✅ 已處理
processed_at: 2025-10-16 17:00:00
retry_count: 0
```

---

### Firestore 同步

**修復前** ❌:
- 數據延遲 30 秒（Cron Job）
- 即時同步無法運作

**修復後** ✅:
- 數據延遲 1-3 秒（即時 Trigger）
- 即時同步正常運作

---

## 🔧 其他修復方案

### 方案 2: 使用自動修復腳本

**如果您不想手動替換 key**:

1. 執行 `d:\repo\supabase\auto_fix_401_error.sql`
2. 腳本會自動嘗試從多個來源獲取 key
3. 如果無法獲取，會跳過即時同步（由 Cron Job 補償）

**優點**:
- ✅ 不需要手動配置
- ✅ 自動降級到 Cron Job

**缺點**:
- ⚠️ 可能無法完全修復（仍需手動配置）

---

### 方案 3: 使用詳細指南

**如果您需要更多細節**:

查看文件: `d:\repo\Supabase即時同步401錯誤_診斷與修復指南.md`

**內容包括**:
- ✅ 詳細的問題診斷
- ✅ 多種修復方案
- ✅ 常見問題解答
- ✅ 修復前後對比

---

## 📊 修復效果

### 性能提升

| 項目 | 修復前 | 修復後 | 提升 |
|------|--------|--------|------|
| 同步延遲 | 30 秒 | 1-3 秒 | **10 倍** |
| HTTP 狀態碼 | 401 | 200 | ✅ |
| 即時同步 | ❌ 無法運作 | ✅ 正常 | ✅ |
| Cron 補償 | ✅ 正常 | ✅ 正常 | - |

---

### 用戶體驗提升

| 場景 | 修復前 | 修復後 |
|------|--------|--------|
| 客戶支付訂金 | 30 秒後看到狀態更新 | **3 秒內**看到狀態更新 |
| 司機確認接單 | 30 秒後客戶收到通知 | **3 秒內**客戶收到通知 |
| 訂單狀態變更 | 30 秒後同步到 Firestore | **3 秒內**同步到 Firestore |

---

## 🎯 下一步

### 1. 啟用即時同步 Trigger

**如果尚未啟用**:

```sql
-- 檢查 Trigger 狀態
SELECT * FROM get_realtime_sync_status();

-- 如果需要啟用，執行：
-- d:\repo\supabase\enable_realtime_sync.sql
```

---

### 2. 測試完整流程

**執行完整的狀態流轉測試**:

```sql
-- 文件: d:\repo\supabase\test_status_flow.sql
```

**預期結果**:
- ✅ 9 個狀態變更
- ✅ 9 個事件已處理
- ✅ 延遲 < 3 秒

---

### 3. 配置管理後台

**在管理後台的「派單設定」頁面**:

1. 找到「即時同步開關」
2. 確認開關狀態
3. 測試開關功能

---

## 📞 需要幫助？

### 如果修復失敗

**檢查清單**:
1. ✅ Service Role Key 是否正確？
2. ✅ 是否已重新連接數據庫？
3. ✅ 是否使用了 **service_role** key（不是 anon key）？

**驗證 key**:
```sql
SELECT 
  LENGTH(current_setting('app.settings.service_role_key', true)) AS key_length,
  SUBSTRING(current_setting('app.settings.service_role_key', true), 1, 10) AS key_prefix;
```

**預期結果**:
- `key_length` > 100
- `key_prefix` = `eyJhbGciOi`

---

### 如果仍然有問題

**查看詳細日誌**:

1. 前往 [Supabase Dashboard](https://supabase.com/dashboard)
2. Edge Functions → sync-to-firestore → Logs
3. 查看最近的錯誤訊息

---

## 📚 相關文件

### 修復腳本
1. ✅ `supabase/QUICK_FIX_401.sql` - **快速修復腳本（推薦）**
2. ✅ `supabase/auto_fix_401_error.sql` - 自動修復腳本
3. ✅ `supabase/fix_401_auth_error.sql` - 手動配置腳本

### 測試腳本
4. ✅ `supabase/test_realtime_trigger_only.sql` - 測試即時 Trigger
5. ✅ `supabase/test_status_flow.sql` - 測試完整狀態流轉

### 文檔
6. ✅ `Supabase即時同步401錯誤_診斷與修復指南.md` - 詳細指南
7. ✅ `401錯誤修復_執行摘要.md` - 本文檔

---

**修復日期**: 2025-10-16  
**修復人員**: AI Assistant  
**修復狀態**: ✅ 腳本已創建，等待執行  
**預計修復時間**: 5 分鐘  
**難度**: ⭐ 簡單

