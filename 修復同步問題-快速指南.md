# 修復同步問題 - 快速指南

**問題**：訂單創建成功，但 App 顯示「訂單不存在」  
**原因**：Edge Function 的欄位名稱不匹配  
**狀態**：✅ 已修復 - 需要部署

---

## 🚀 立即執行（5 分鐘）

### 步驟 1：部署 Edge Function（2 分鐘）

```bash
cd supabase
./deploy-edge-function-fix.sh
```

或手動：
```bash
supabase functions deploy sync-to-firestore --project-ref vlyhwegpvpnjyocqmfqc
```

---

### 步驟 2：手動觸發測試（1 分鐘）

1. 打開 Supabase Dashboard
2. Edge Functions → sync-to-firestore
3. 點擊「Invoke」按鈕

**預期結果**：
```json
{
  "message": "事件處理完成",
  "total": 1,
  "success": 1,
  "failure": 0
}
```

---

### 步驟 3：驗證同步（2 分鐘）

**在 Supabase SQL Editor 執行**：
```sql
SELECT 
  id,
  payload->>'bookingNumber' as booking_number,
  created_at,
  processed_at,
  CASE 
    WHEN processed_at IS NOT NULL THEN '✅ 已同步'
    ELSE '❌ 未同步'
  END as status
FROM outbox
WHERE created_at >= NOW() - INTERVAL '10 minutes'
ORDER BY created_at DESC;
```

**預期**：`status` 顯示 '✅ 已同步'

---

### 步驟 4：測試 App（1 分鐘）

1. 打開手機 App
2. 查看訂單詳情
3. 確認不再顯示「訂單不存在」

---

## ✅ 成功標準

- [x] Edge Function 部署成功
- [x] 手動觸發返回 success: 1
- [x] processed_at 有值
- [x] App 可以查看訂單

---

## 🔧 如果失敗

### 問題：部署失敗

```bash
# 登入
supabase login

# 重新部署
supabase functions deploy sync-to-firestore --project-ref vlyhwegpvpnjyocqmfqc
```

### 問題：processed_at 仍是 NULL

1. 檢查 Edge Function 日誌（Supabase Dashboard）
2. 查看錯誤訊息
3. 參考 `supabase/SYNC_FIX_TESTING_GUIDE.md`

### 問題：App 仍顯示「訂單不存在」

1. 等待 30 秒（Cron Job 執行）
2. 檢查 Firestore（Firebase Console）
3. 重啟 App

---

## 📚 詳細文檔

| 需求 | 文檔 |
|------|------|
| **快速指南** | `修復同步問題-快速指南.md`（本文檔）⭐ |
| **詳細測試** | `supabase/SYNC_FIX_TESTING_GUIDE.md` |
| **診斷查詢** | `supabase/diagnose-sync-issue.sql` |
| **開發歷程** | `docs/20251004_2246_04_訂單同步問題修復.md` |

---

**預計時間**：5 分鐘  
**難度**：簡單  
**狀態**：準備部署

