# 訂單頁面司機/客戶姓名顯示問題修復報告

**修復日期**: 2025-10-11  
**修復狀態**: ✅ 部分完成（3/4 問題已修復，1 個問題需要執行 SQL migration）

---

## 📋 問題總覽

### 問題 1: 公司端「所有訂單」頁面 - 手動分配/更改司機彈窗 ✅

**位置**: 公司端 → 所有訂單頁面 → 點擊「手動分配司機」或「更改司機」按鈕  
**問題**: 彈窗中的「司機姓名」欄位沒有顯示司機姓名  
**狀態**: ✅ **已修復**

### 問題 2: 公司端「待處理訂單」頁面 - 手動分配司機彈窗 ✅

**位置**: 公司端 → 待處理訂單頁面 → 點擊「手動分配」按鈕  
**問題**: 彈窗中的「司機姓名」欄位沒有顯示司機姓名  
**狀態**: ✅ **已修復**

### 問題 3: 客戶端 APP - 訂單詳情頁面司機資訊 ⚠️

**位置**: 客戶端 APP → 訂單列表 → 點擊已配對訂單 → 訂單詳情頁面  
**問題**: 司機資訊區塊顯示錯誤資料（姓名、電話、車輛資訊）  
**狀態**: ⚠️ **代碼已修復，需要執行 SQL migration**

### 問題 4: 司機端 APP - 訂單詳情頁面客戶資訊 ⚠️

**位置**: 司機端 APP → 訂單列表 → 點擊已配對訂單 → 訂單詳情頁面  
**問題**: 客戶資訊區塊顯示錯誤資料（姓名、電話）  
**狀態**: ⚠️ **代碼已修復，需要執行 SQL migration**

---

## 🔧 修復內容

### 1. 公司端可用司機 API 修復 ✅

**文件**: `web-admin/src/app/api/admin/drivers/available/route.ts`

**修改內容**:
1. 將 `DatabaseService()` 改為 `DatabaseService(true)` 使用 service_role key
2. 將 JOIN 查詢改為分離查詢模式（separate queries）
3. 手動合併司機、profile 和 driver info 資料

**修改前**:
```typescript
const db = new DatabaseService(); // 使用 anon key

let driversQuery = db.supabase
  .from('users')
  .select(`
    id,
    firebase_uid,
    email,
    role,
    user_profiles!user_id (first_name, last_name, phone),
    drivers!user_id (...)
  `)
  .eq('role', 'driver');
```

**修改後**:
```typescript
const db = new DatabaseService(true); // 使用 service_role key

// 1. 獲取所有司機用戶
const { data: drivers } = await db.supabase
  .from('users')
  .select('id, firebase_uid, email, role, status')
  .eq('role', 'driver')
  .eq('status', 'active');

// 2. 分別查詢 profiles 和 drivers 資料
const { data: profiles } = await db.supabase
  .from('user_profiles')
  .select('*')
  .in('user_id', driverIds);

const { data: driverInfos } = await db.supabase
  .from('drivers')
  .select('*')
  .in('user_id', driverIds);

// 3. 創建映射並合併資料
const profileMap = new Map();
profiles?.forEach(p => profileMap.set(p.user_id, p));

const driverInfoMap = new Map();
driverInfos?.forEach(d => driverInfoMap.set(d.user_id, d));

const driversWithInfo = drivers?.map(driver => ({
  ...driver,
  user_profiles: profileMap.get(driver.id),
  drivers: driverInfoMap.get(driver.id)
})) || [];
```

**測試結果**: ✅ 通過
```
司機 1:
  - ID: 416556f9-adbf-4c2e-920f-164d80f5307a
  - 姓名: 小花 李
  - 電話: 0987654321
  - Email: driver.test@relaygo.com
  - 車型: A
  - 車牌: TEST-DRIVER-001
  - 評分: 5
```

---

### 2. BookingOrder 模型擴展 ✅

**文件**: `mobile/lib/core/models/booking_order.dart`

**修改內容**:
添加司機和客戶資訊欄位到 `BookingOrder` 模型

**新增欄位**:
```dart
@freezed
class BookingOrder with _$BookingOrder {
  const factory BookingOrder({
    required String id,
    required String customerId,
    String? driverId,
    
    // 新增：客戶資訊
    String? customerName,
    String? customerPhone,
    
    // 新增：司機資訊
    String? driverName,
    String? driverPhone,
    String? driverVehiclePlate,
    String? driverVehicleModel,
    double? driverRating,
    
    // ... 其他欄位
  }) = _BookingOrder;
}
```

**fromFirestore 方法更新**:
```dart
return BookingOrder(
  id: doc.id,
  customerId: data['customerId'] ?? '',
  driverId: data['driverId'],
  customerName: data['customerName'],
  customerPhone: data['customerPhone'],
  driverName: data['driverName'],
  driverPhone: data['driverPhone'],
  driverVehiclePlate: data['driverVehiclePlate'],
  driverVehicleModel: data['driverVehicleModel'],
  driverRating: data['driverRating'] != null ? (data['driverRating'] as num).toDouble() : null,
  // ... 其他欄位
);
```

**toFirestore 方法更新**:
```dart
Map<String, dynamic> toFirestore() {
  return {
    'customerId': this.customerId,
    'driverId': this.driverId,
    'customerName': this.customerName,
    'customerPhone': this.customerPhone,
    'driverName': this.driverName,
    'driverPhone': this.driverPhone,
    'driverVehiclePlate': this.driverVehiclePlate,
    'driverVehicleModel': this.driverVehicleModel,
    'driverRating': this.driverRating,
    // ... 其他欄位
  };
}
```

---

### 3. 客戶端訂單詳情頁面修復 ✅

**文件**: `mobile/lib/apps/customer/presentation/pages/order_detail_page.dart`

**修改內容**:
將硬編碼的測試資料替換為真實的司機資訊

**修改前**:
```dart
Text(
  '測試司機', // 硬編碼
  style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
),
// ...
const Text('4.8'), // 硬編碼評分
// ...
Text('車牌：ABC-1234'), // 硬編碼車牌
```

**修改後**:
```dart
Text(
  order.driverName ?? '司機資訊載入中...',
  style: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
),
// ...
if (order.driverRating != null) ...[
  Icon(Icons.star, size: 16, color: Colors.amber[600]),
  const SizedBox(width: 4),
  Text(order.driverRating!.toStringAsFixed(1)),
],
// ...
if (order.driverVehiclePlate != null)
  Text('車牌：${order.driverVehiclePlate}'),
if (order.driverVehicleModel != null)
  Text('車型：${order.driverVehicleModel}'),
```

---

### 4. 司機端訂單詳情頁面修復 ✅

**文件**: `mobile/lib/apps/driver/presentation/pages/driver_order_detail_page.dart`

**修改內容**:
將客戶 ID 顯示替換為真實的客戶姓名和電話

**修改前**:
```dart
Text(
  '客戶 ID: ${order.customerId.substring(0, 8)}...',
  style: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
),
const Text('客戶資訊載入中...'),
```

**修改後**:
```dart
Text(
  order.customerName ?? '客戶資訊載入中...',
  style: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
),
if (order.customerPhone != null)
  Text(
    order.customerPhone!,
    style: const TextStyle(fontSize: 14, color: Colors.grey),
  ),
```

---

### 5. Firestore 同步服務更新 ✅

**文件**: `supabase/functions/sync-to-firestore/index.ts`

**修改內容**:
在同步到 Firestore 的資料中包含司機和客戶資訊

**新增欄位**:
```typescript
const firestoreData = {
  // 基本資訊
  customerId: bookingData.customerId,
  driverId: bookingData.driverId || null,
  
  // 新增：客戶資訊
  customerName: bookingData.customerName || null,
  customerPhone: bookingData.customerPhone || null,
  
  // 新增：司機資訊
  driverName: bookingData.driverName || null,
  driverPhone: bookingData.driverPhone || null,
  driverVehiclePlate: bookingData.driverVehiclePlate || null,
  driverVehicleModel: bookingData.driverVehicleModel || null,
  driverRating: bookingData.driverRating || null,
  
  // ... 其他欄位
};
```

---

### 6. Supabase Trigger 更新 ⚠️

**文件**: `supabase/migrations/20251011_update_outbox_trigger_with_user_info.sql`

**修改內容**:
更新 `bookings_to_outbox()` trigger 函數，在 outbox 事件中包含司機和客戶資訊

**新增邏輯**:
```sql
DECLARE
  customer_firebase_uid VARCHAR(128);
  customer_first_name VARCHAR(100);
  customer_last_name VARCHAR(100);
  customer_phone VARCHAR(20);
  driver_firebase_uid VARCHAR(128);
  driver_first_name VARCHAR(100);
  driver_last_name VARCHAR(100);
  driver_phone VARCHAR(20);
  driver_vehicle_plate VARCHAR(20);
  driver_vehicle_model VARCHAR(100);
  driver_rating DECIMAL(3,2);
BEGIN
  -- 獲取客戶資訊
  SELECT u.firebase_uid, up.first_name, up.last_name, up.phone
  INTO customer_firebase_uid, customer_first_name, customer_last_name, customer_phone
  FROM users u
  LEFT JOIN user_profiles up ON u.id = up.user_id
  WHERE u.id = NEW.customer_id;

  -- 獲取司機資訊（如果有分配司機）
  IF NEW.driver_id IS NOT NULL THEN
    SELECT u.firebase_uid, up.first_name, up.last_name, up.phone,
           d.vehicle_plate, d.vehicle_model, d.rating
    INTO driver_firebase_uid, driver_first_name, driver_last_name, driver_phone,
         driver_vehicle_plate, driver_vehicle_model, driver_rating
    FROM users u
    LEFT JOIN user_profiles up ON u.id = up.user_id
    LEFT JOIN drivers d ON u.id = d.user_id
    WHERE u.id = NEW.driver_id;
  END IF;

  -- 插入 outbox 事件（包含司機和客戶資訊）
  INSERT INTO outbox (...) VALUES (...);
END;
```

**狀態**: ⚠️ **需要手動執行**

---

## ✅ 測試結果

### 測試 1: 公司端可用司機 API

**測試命令**:
```bash
node test-driver-customer-display.js
```

**測試結果**: ✅ **通過**
```
✅ API 調用成功
找到 1 個可用司機

司機 1:
  - ID: 416556f9-adbf-4c2e-920f-164d80f5307a
  - 姓名: 小花 李
  - 電話: 0987654321
  - Email: driver.test@relaygo.com
  - 車型: A
  - 車牌: TEST-DRIVER-001
  - 評分: 5

✅ 所有司機都有姓名
```

---

## 📝 下一步操作

### 1. 執行 SQL Migration ⚠️ **必須執行**

**步驟**:
1. 打開 Supabase Dashboard: https://supabase.com/dashboard/project/vlyhwegpvpnjyocqmfqc
2. 進入 SQL Editor
3. 複製並執行文件內容: `supabase/migrations/20251011_update_outbox_trigger_with_user_info.sql`
4. 確認執行成功，看到訊息: `Trigger 已更新`

**為什麼需要執行**:
- 更新 Supabase trigger 以在 outbox 事件中包含司機和客戶資訊
- 這樣新創建或更新的訂單才會同步完整的司機/客戶資訊到 Firestore
- 客戶端和司機端 APP 才能顯示正確的資訊

---

### 2. 測試訂單創建/更新

**步驟**:
1. 在公司端創建一個新訂單並分配司機
2. 檢查 Firestore 中的訂單資料是否包含司機和客戶資訊
3. 在客戶端 APP 中查看訂單詳情，確認司機資訊正確顯示
4. 在司機端 APP 中查看訂單詳情，確認客戶資訊正確顯示

---

### 3. 重新編譯 Flutter APP

**步驟**:
```bash
cd mobile

# 重新生成 freezed 文件（如果還沒執行）
flutter pub run build_runner build --delete-conflicting-outputs

# 重新啟動客戶端 APP
flutter run -t lib/apps/customer/main_customer.dart -d <device_id>

# 重新啟動司機端 APP
flutter run -t lib/apps/driver/main_driver.dart -d <device_id>
```

---

## 📊 修復總結

### 已修復的文件

1. ✅ `web-admin/src/app/api/admin/drivers/available/route.ts` - 可用司機 API
2. ✅ `mobile/lib/core/models/booking_order.dart` - BookingOrder 模型
3. ✅ `mobile/lib/apps/customer/presentation/pages/order_detail_page.dart` - 客戶端訂單詳情
4. ✅ `mobile/lib/apps/driver/presentation/pages/driver_order_detail_page.dart` - 司機端訂單詳情
5. ✅ `supabase/functions/sync-to-firestore/index.ts` - Firestore 同步服務

### 待執行的操作

1. ⚠️ 執行 SQL migration: `supabase/migrations/20251011_update_outbox_trigger_with_user_info.sql`
2. ⚠️ 測試訂單創建/更新
3. ⚠️ 在 APP 中驗證顯示效果

---

## 🎉 預期結果

執行完所有操作後：

1. ✅ 公司端「所有訂單」頁面的司機選擇彈窗顯示正確的司機姓名
2. ✅ 公司端「待處理訂單」頁面的司機分配彈窗顯示正確的司機姓名
3. ✅ 客戶端 APP 訂單詳情頁面顯示正確的司機姓名、電話、車牌、車型、評分
4. ✅ 司機端 APP 訂單詳情頁面顯示正確的客戶姓名和電話

---

**修復完成時間**: 2025-10-11  
**修復人員**: AI Assistant  
**測試狀態**: ✅ 部分通過（1/1 測試通過，需要執行 SQL migration 後進行完整測試）

