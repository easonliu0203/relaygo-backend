# 異常訂單診斷報告

> **訂單 ID**: `9452ee79-8e43-492e-a89d-8eb4d7d3ae90`  
> **訂單編號**: `FINAL_1760637491`  
> **診斷日期**: 2025-10-16  
> **診斷結果**: ✅ **這是測試腳本自動創建的訂單，不是異常**

---

## 🔍 **問題診斷**

### 1. 訂單來源

**結論**: ✅ **這筆訂單是由 `FINAL_FIX_401.sql` 測試腳本自動創建的**

**證據**:

#### 證據 1: 訂單編號前綴
```
訂單編號: FINAL_1760637491
前綴: FINAL_
時間戳: 1760637491 (對應 2025-10-16 17:58:11 UTC)
```

**對應代碼** (`supabase/FINAL_FIX_401.sql` 第 133 行):
```sql
INSERT INTO bookings (
  ...
  booking_number,
  ...
) VALUES (
  ...
  'FINAL_' || EXTRACT(EPOCH FROM NOW())::BIGINT,  -- ← 這裡！
  ...
)
```

#### 證據 2: 訂單詳情
```
上車地點: 測試地點 - 最終修復
目的地: 測試目的地
基礎價格: 1500.00
訂金: 450.00
```

**對應代碼** (`supabase/FINAL_FIX_401.sql` 第 139-145 行):
```sql
VALUES (
  ...
  '測試地點 - 最終修復',  -- ← 完全一致！
  25.0330,
  121.5654,
  '測試目的地',          -- ← 完全一致！
  1500.00,               -- ← 完全一致！
  1500.00,
  450.00                 -- ← 完全一致！
)
```

#### 證據 3: 創建時間
```
訂單創建時間: 2025-10-16 17:58:10 UTC
腳本執行時間: 2025-10-16 17:58:xx UTC（您執行 FINAL_FIX_401.sql 的時間）
```

**時間完全吻合！**

---

### 2. 狀態變更原因

**問題**: 為什麼訂單會自動從 `pending_payment` 變更為 `paid_deposit`？

**答案**: ✅ **這是測試腳本的預期行為**

**腳本邏輯** (`supabase/FINAL_FIX_401.sql` 第 154-158 行):
```sql
-- 更新訂單狀態以觸發 Trigger
UPDATE bookings
SET status = 'paid_deposit',
    updated_at = NOW()
WHERE id = test_booking_id;
```

**目的**: 測試即時同步 Trigger 是否正常工作

**時間線**:
```
17:58:10 - 訂單創建（status = 'pending_payment'）
17:58:11 - 腳本執行 UPDATE（status = 'paid_deposit'）
17:58:11 - Trigger 觸發，發送 HTTP 請求到 Edge Function
17:58:11 - Edge Function 同步數據到 Firestore
```

---

### 3. 訂單取消原因

**問題**: 為什麼訂單會被取消？取消原因代碼 `6546` 是什麼？

**答案**: ⚠️ **需要查詢數據庫確認**

**可能的原因**:

#### 可能性 1: 手動取消 ⭐
- 您可能在管理後台或客戶端手動取消了這筆訂單
- 取消時間：18:00:39（訂單創建後 2 分 29 秒）
- 取消原因：`6546`（這不是標準的取消原因格式）

#### 可能性 2: 自動清理
- 可能有自動清理測試訂單的邏輯
- 但是代碼庫中沒有找到相關邏輯

#### 可能性 3: 測試操作
- 您可能在測試取消訂單功能時取消了這筆訂單
- `6546` 可能是您輸入的測試文本

**驗證方法**:
執行查詢腳本 `supabase/查詢異常訂單.sql` 查看詳細的取消原因。

---

### 4. 取消原因代碼 `6546` 的含義

**問題**: `6546` 代表什麼？

**答案**: ❓ **這不是系統預定義的取消原因代碼**

**系統設計**:
- 取消原因欄位類型：`TEXT`（文本）
- 預期格式：「行程有變，需要取消」、「測試取消功能」等
- 不是數字代碼

**可能的解釋**:
1. **手動輸入的測試文本**：您可能在測試時輸入了 `6546`
2. **錯誤的輸入**：可能是誤操作
3. **其他系統的代碼**：可能是從其他系統複製過來的

**驗證方法**:
```sql
SELECT cancellation_reason
FROM bookings
WHERE id = '9452ee79-8e43-492e-a89d-8eb4d7d3ae90';
```

---

## 📊 **完整時間線**

### 訂單生命週期

```
2025-10-16 17:58:10 UTC
  ↓
  ✅ 訂單創建（由 FINAL_FIX_401.sql 腳本）
  - 訂單編號: FINAL_1760637491
  - 狀態: pending_payment
  - 客戶 ID: c03f0310-d3c8-44ab-8aec-1a4a858c52cb
  ↓
2025-10-16 17:58:11 UTC
  ↓
  ✅ 狀態更新（由 FINAL_FIX_401.sql 腳本）
  - 狀態: pending_payment → paid_deposit
  - Trigger 觸發
  - HTTP 請求發送到 Edge Function
  - 數據同步到 Firestore
  ↓
2025-10-16 18:00:39 UTC（2 分 29 秒後）
  ↓
  ⚠️  訂單取消（原因未知）
  - 狀態: paid_deposit → cancelled
  - 取消原因: 6546
  - 取消時間: 2025-10-16 18:00:39.397+00
```

---

## ✅ **結論**

### 1. 訂單創建

**問題**: 為什麼會自動創建這筆訂單？

**答案**: ✅ **這是 `FINAL_FIX_401.sql` 測試腳本的預期行為**

**證據**:
- ✅ 訂單編號前綴 `FINAL_` 與腳本一致
- ✅ 訂單詳情（地點、價格）與腳本一致
- ✅ 創建時間與腳本執行時間一致

**這不是異常，這是測試腳本的正常行為！**

---

### 2. 狀態變更

**問題**: 為什麼訂單會自動從 `pending_payment` 變更為 `paid_deposit`？

**答案**: ✅ **這是測試腳本的預期行為**

**目的**: 測試即時同步 Trigger 是否正常工作

**腳本邏輯**:
1. 創建訂單（pending_payment）
2. 等待 1 秒
3. 更新狀態為 paid_deposit
4. 觸發 Trigger
5. 發送 HTTP 請求到 Edge Function
6. 同步數據到 Firestore

**這是測試流程的一部分！**

---

### 3. 訂單取消

**問題**: 為什麼訂單會被取消？

**答案**: ⚠️ **需要進一步調查**

**可能的原因**:
1. **手動取消**：您可能在管理後台或客戶端手動取消了這筆訂單
2. **測試操作**：您可能在測試取消訂單功能時取消了這筆訂單
3. **自動清理**：可能有自動清理測試訂單的邏輯（但代碼庫中未找到）

**驗證方法**:
執行 `supabase/查詢異常訂單.sql` 查看詳細的取消原因和相關事件。

---

### 4. 取消原因代碼 `6546`

**問題**: `6546` 代表什麼？

**答案**: ❓ **這不是系統預定義的取消原因代碼**

**可能的解釋**:
1. 手動輸入的測試文本
2. 錯誤的輸入
3. 其他系統的代碼

**建議**: 查詢數據庫確認實際的取消原因文本。

---

## 🛠️ **解決方案**

### 1. 清理測試訂單

**問題**: 如何防止在手動測試時出現這種自動創建的訂單？

**答案**: 定期清理測試訂單

**清理腳本**:
```sql
-- 刪除所有 FINAL_ 開頭的測試訂單
DELETE FROM bookings
WHERE booking_number LIKE 'FINAL_%';

-- 或者只刪除已取消的測試訂單
DELETE FROM bookings
WHERE booking_number LIKE 'FINAL_%'
  AND status = 'cancelled';
```

---

### 2. 修改測試腳本

**建議**: 在測試腳本中添加自動清理邏輯

**修改 `FINAL_FIX_401.sql`**:
```sql
-- 在腳本開始時，清理舊的測試訂單
DELETE FROM bookings
WHERE booking_number LIKE 'FINAL_%'
  AND created_at < NOW() - INTERVAL '1 hour';

-- 然後創建新的測試訂單
INSERT INTO bookings (...) VALUES (...);
```

---

### 3. 使用不同的測試前綴

**建議**: 為不同的測試使用不同的前綴

**範例**:
- `FINAL_` - 401 錯誤修復測試
- `TEST_` - 一般功能測試
- `FLOW_` - 狀態流轉測試
- `TRG_` - Trigger 測試

**好處**:
- 容易識別測試訂單的來源
- 可以針對性地清理特定類型的測試訂單

---

## 📚 **相關文件**

### 測試腳本
1. ✅ `supabase/FINAL_FIX_401.sql` - 401 錯誤修復測試腳本（創建了這筆訂單）
2. ✅ `supabase/test_status_flow.sql` - 狀態流轉測試腳本
3. ✅ `supabase/test_realtime_trigger_only.sql` - Trigger 測試腳本

### 查詢腳本
4. ✅ `supabase/查詢異常訂單.sql` - 查詢異常訂單詳情

### 診斷報告
5. ✅ `異常訂單診斷報告_FINAL_1760637491.md` - 本文檔

---

## 🎯 **下一步**

### 1. 查詢訂單詳情

**執行**: `supabase/查詢異常訂單.sql`

**目的**: 確認取消原因和相關事件

---

### 2. 清理測試訂單

**執行**:
```sql
DELETE FROM bookings
WHERE booking_number LIKE 'FINAL_%';
```

**目的**: 清理所有測試訂單

---

### 3. 繼續測試

**執行**: `supabase/test_status_flow.sql`

**目的**: 測試完整的狀態流轉功能

---

## 📞 **需要幫助？**

### 如果仍有疑問

**請提供以下信息**:
1. `supabase/查詢異常訂單.sql` 的執行結果
2. 您是否手動取消了這筆訂單？
3. 您是否在測試取消訂單功能？

---

**診斷日期**: 2025-10-16  
**診斷人員**: AI Assistant  
**診斷結果**: ✅ 這是測試腳本的預期行為，不是異常  
**建議**: 定期清理測試訂單

